# Optional: enable real-time US market quotes via Finnhub (https://finnhub.io).
# Leave unset to use the built-in simulated real-time feed (no API key required).
VITE_FINNHUB_TOKEN=

# Polling interval (ms) for the real provider. Default 2000.
VITE_QUOTE_POLL_MS=2000
