# deps
node_modules/

# build intermediates & QA artifacts
build/
src/**/*.map

# generated single-file bundle during development; the final build is
# force-added as the playable deliverable in the release commit
/index.html

# os / editor
.DS_Store
*.swp
