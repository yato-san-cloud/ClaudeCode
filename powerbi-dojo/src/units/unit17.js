window.DOJO_UNITS = window.DOJO_UNITS || [];
window.DOJO_UNITS.push({
  id: "unit17",
  no: 7,
  track: "advanced",
  icon: "🚀",
  name: "M応用 II: 堅牢化と大規模データ",
  color: "#8a5a44",
  desc: "クエリフォールディング・エラー処理・パラメータ設計・増分更新・パフォーマンス戦略まで、「販売サンプル」モデルで大規模データにも耐えるPower Queryの組み方を学ぶ",
  lessons: [
    {
      title: "クエリフォールディング詳説",
      doc: `
<p>営業部門のデータベースには売上テーブルが数千万行あり、Power Queryで「今年の行だけ、必要な3列だけ」に絞ってから読み込みたいとします。このとき、Power Queryが本当に数千万行すべてを一度手元に読み込んでからフィルターしているとしたら、更新は非現実的な時間がかかってしまいます。実際にはPower Queryには<b>クエリフォールディング(Query Folding)</b>という仕組みがあり、多くの場合はフィルターや列選択の処理をデータソース側(SQLサーバーなど)にそのまま「押し戻して」実行させ、必要な結果だけを受け取ります。</p>
<p>フォールディングが効くかどうかはステップ内容次第です。<b>Table.SelectRows</b>による行フィルターや<b>Table.SelectColumns</b>による列選択、<b>Table.Sort</b>による並べ替え、単純な<b>Table.Group</b>による集計などは、リレーショナルデータベースの多くでSQLのWHERE句・SELECT句・ORDER BY句・GROUP BY句に変換できるため折りたたまれやすい操作です。一方、<b>List.Generate</b>を使った独自ループやカスタム関数を使った複雑な行ごとの変換など、SQLに素直に翻訳できないM固有のロジックはフォールディングを打ち切ってしまいます。ステップを右クリックして「ネイティブクエリの表示」を選ぶと、その時点までの処理が実際にどんなSQLとして送られているかを確認でき、この項目がグレーアウトしていれば、そこまでの処理はもうソース側に折りたたまれていない(=ローカルのPower Queryエンジン側で実行されている)ことを意味します。</p>
<pre>// ステップを右クリック→「ネイティブクエリの表示」で、
// ここまでの処理が実際にどんなSQLに変換されているか確認できる
= Table.SelectRows(Source, each [年] = 2026)

// フォールディングを保つコツ: 折りたためる操作(フィルター・列選択)を先に、
// 折りたためない独自ロジックは最後にまとめる
let
    Source = ...,
    絞り込み = Table.SelectRows(Source, each [年] = 2026),      // ← 先に絞り込む(折りたたまれやすい)
    列選択 = Table.SelectColumns(絞り込み, {"商品ID", "金額"}), // ← 次に列を絞る(折りたたまれやすい)
    追加 = Table.AddColumn(列選択, "割引後", each 独自関数([金額]))  // ← 独自ロジックは最後に
in
    追加</pre>
<p>データソース側で対応しきれない複雑な条件をどうしてもSQLとして書きたい場合は、<b>Value.NativeQuery</b>を使ってSQL文をそのまま渡すこともできます。第4引数のオプションレコードで<code>EnableFolding = true</code>を指定すると、そのネイティブクエリより後ろに続くステップについても、可能な範囲でさらにフォールディングを試みるよう指示できます。ただしネイティブクエリは記述ミスやSQLインジェクションのリスクを自分で背負うことになるため、まずは標準のTable.SelectRowsやTable.SelectColumnsで素直に書き、それでフォールディングが効かない場合の最終手段として検討するのが安全です。</p>
<p class="tip">💡 実務メモ: フォールディングが途中で切れると、以降のすべてのステップがローカル側の処理になり、巨大なテーブルを丸ごとメモリに展開しかねません。更新が急に遅くなったと感じたら、まずステップを1つずつ右クリックして「ネイティブクエリの表示」がどこで消えるかを確認し、その直前のステップまでに折りたためない処理が紛れ込んでいないか点検しましょう。</p>`,
      exercises: [
        {
          type: "mc",
          q: "クエリフォールディング(Query Folding)の説明として正しいものはどれですか?",
          c: ["Power Queryのステップ(フィルター・列選択・集計など)を、可能な範囲でデータソース側のネイティブな問い合わせ(SQLなど)に変換し、処理をソース側に実行させる仕組み", "すべてのステップを必ずPower Query側のローカルメモリ上で実行する仕組み", "クエリの複数ステップを1つの見た目にまとめて表示するだけのUI機能", "Excelファイルを読み込むときにだけ働く特別な最適化機能"],
          a: 0,
          e: "クエリフォールディングは、Power Queryのステップを可能な限りソース側のネイティブな問い合わせに変換し、フィルターや集計をソース側で実行させる仕組みです。これにより不要なデータを手元に転送せずに済み、大規模データでも更新が現実的な時間で終わります。"
        },
        {
          type: "mc",
          q: "次のうち、クエリフォールディングを妨げやすい(=折りたたみを打ち切りやすい)操作はどれですか?",
          c: ["List.Generateを使った独自ループでカスタム列を追加する", "Table.SelectRowsによる単純な行フィルター", "Table.SelectColumnsによる列選択", "Table.Sortによる並べ替え"],
          a: 0,
          e: "List.Generateのような独自ループやM固有のロジックは、多くのデータソースのSQL構文に素直に翻訳できないため、フォールディングを打ち切ってしまいます。単純なフィルター・列選択・並べ替えは折りたたまれやすい操作です。"
        },
        {
          type: "mc",
          q: "あるステップを右クリックし「ネイティブクエリの表示」を選ぼうとしたところ、この項目がグレーアウトして選択できませんでした。これが意味することとして正しいものはどれですか?",
          c: ["現在のステップまでの処理がソース側に折りたたまれておらず、ローカルのPower Queryエンジン側で実行されていることを示す", "データソースへの接続が切断されていることを示す", "プレビューが1000行制限に達していることを示す", "クエリの数式に文法エラーがあることを示す"],
          a: 0,
          e: "「ネイティブクエリの表示」は、そこまでの処理がソース側にフォールディングされている場合にのみ選択できます。グレーアウトしている場合は、その時点までにフォールディングが途切れ、ローカル側の処理に切り替わっていることを意味します。"
        },
        {
          type: "mc",
          q: "クエリフォールディングをできるだけ長く保つためのステップ順序の工夫として適切なものはどれですか?",
          c: ["Table.SelectRowsやTable.SelectColumnsなど折りたためる操作をできるだけ先に行い、独自関数やList.Generateのような折りたためない操作は後段にまとめる", "ステップの順序はフォールディングの可否に一切影響しないため、書きやすい順に自由に並べてよい", "折りたためない独自ロジックを先頭に置けば、以降のステップはすべて自動的にソース側へ折りたたまれる", "フォールディングを保つには、すべてのステップを必ずValue.NativeQueryで書き換える必要がある"],
          a: 0,
          e: "折りたためる操作を先に配置すれば、その分ソース側で絞り込みが済んだ状態から後続の処理が始まります。逆に折りたためない操作を先頭に置くと、それ以降のすべてのステップがローカル処理になってしまいます。"
        },
        {
          type: "fill",
          q: "テーブル「Source」に対し、SQL文をネイティブクエリとして実行し、以降のステップでもフォールディングを試みるよう指示するM式の空欄を埋めてください。",
          lang: "m",
          code: "Value.NativeQuery(Source, \"SELECT * FROM 売上\", 【0】, [EnableFolding = 【1】])",
          bank: ["null", "true", "false", "{}"],
          answers: [0, 1],
          e: "第3引数のパラメータを使わない場合はnullを渡します。第4引数のオプションレコードでEnableFolding = trueを指定すると、このネイティブクエリより後ろのステップについても可能な範囲でさらにフォールディングが試みられます。"
        },
        {
          type: "type",
          q: "テーブル「Source」に対し、SQL文 \"SELECT 商品ID, 金額 FROM 売上\" をパラメータなしのネイティブクエリとして実行し、フォールディングを有効にするM式を、Value.NativeQueryを使って書いてください。",
          lang: "m",
          answer: "Value.NativeQuery(Source, \"SELECT 商品ID, 金額 FROM 売上\", null, [EnableFolding = true])",
          hint: "Value.NativeQuery(接続, \"SQL文\", パラメータ, [EnableFolding = true])という順番です。パラメータを使わない場合はnullを渡します。",
          e: "Value.NativeQueryは接続・SQL文・パラメータ・オプションの順で引数を取ります。EnableFolding = trueを指定することで、ネイティブクエリの結果に対する後続ステップもソース側への折りたたみが試みられます。"
        }
      ]
    },
    {
      title: "エラー処理設計",
      doc: `
<p>経理部から毎月届くCSVファイルの「金額」列に、まれに「未入力」や全角文字が混じっていて、そのまま数値変換しようとすると更新がエラーで止まってしまう——こうした「たまに汚れるデータ」への備えは、実務のPower Queryでは避けて通れないテーマです。M言語では、エラーが起きそうな式を<b>try</b>で包むことで、エラーを検知しつつ処理を続行できます。</p>
<p><b>try 式 otherwise 代替値</b>という書き方をすると、式の評価でエラーが発生した場合にだけotherwise側の値が使われ、エラーがなければ本来の値がそのまま使われます。otherwiseを付けずに<b>try 式</b>だけを書くと、結果は値そのものではなく<code>HasError</code>・<code>Value</code>(エラーがない場合)・<code>Error</code>(エラーがある場合)という3つのフィールドを持つ<b>レコード</b>になります。さらに<code>Error</code>フィールドの中には<code>Reason</code>(エラー種別)・<code>Message</code>(人が読める説明文)・<code>Detail</code>(問題の値など)という3つの詳細フィールドが入っており、これらを調べれば「なぜ失敗したか」を具体的に特定できます。</p>
<pre>// 数値変換に失敗したらnullを返す(単純な代替値)
Table.AddColumn(ソース, "金額数値", each try Number.From([金額]) otherwise null)

// try単体の結果はレコード。HasErrorとMessageを見て内容を確認する
検証 = try Number.From([金額]),
結果 = if 検証[HasError] then 検証[Error][Message] else Text.From(検証[Value])

// 列単位でエラー行を除外する(第2引数はエラーを見る対象列)
Table.RemoveRowsWithErrors(変換後, {"金額数値"})</pre>
<p>ここで注意したいのは、<b>try...otherwiseはエラー時の代わりの値を用意するだけで、元データの誤りそのものを修正するわけではない</b>という点です。null埋めのまま気づかずに集計すると、実際には存在した数字がなかったことになり、レポートの数値が静かに間違っていく危険があります。品質を本当に守りたい場面では、エラー行やnull行の件数を数えて基準を超えたら<b>error式</b>で意図的に評価を失敗させ、更新そのものを止めてしまう「品質ゲート」を組み込むのが有効です。更新が失敗すれば、レポートが古いデータのまま静かに使われ続ける事故を防げます。</p>
<pre>// エラー件数が閾値を超えたら更新自体を意図的に失敗させる品質ゲート
エラー件数 = Table.RowCount(Table.SelectRows(変換後, each [金額数値] = null)),
検証済み = if エラー件数 > 100
    then error "品質ゲート: 金額を数値変換できない行が " & Text.From(エラー件数) & " 件あります"
    else 変換後</pre>
<p class="tip">💡 実務メモ: try...otherwiseで静かにnull化するかerrorで止めるかは、「多少の欠損は許容してでも更新を止めたくないデータ」か「1件でも紛れ込んだら気づく必要があるデータ」かで使い分けます。金額のような重要指標には品質ゲートを、住所の表記ゆれのような軽微な項目にはtry...otherwiseを、という具合に対象ごとに設計しましょう。</p>`,
      exercises: [
        {
          type: "mc",
          q: "M言語の try 式 otherwise 代替値 という書き方の動作として正しいものはどれですか?",
          c: ["式の評価でエラーが発生した場合にだけotherwise側の値が使われ、エラーがなければ本来の値がそのまま使われる", "式が正常に評価されてもされなくても、常にotherwise側の値が使われる", "エラーが発生した箇所を自動的に0に置き換え、以降その列全体を数値型に固定する", "try単体でエラーの原因データそのものを自動的に修正する"],
          a: 0,
          e: "try...otherwiseはエラー発生時のみ代替値に切り替わる仕組みで、エラーがなければ元の式の結果がそのまま使われます。あくまで評価結果の切り替えであり、元データを書き換えるものではありません。"
        },
        {
          type: "mc",
          q: "otherwiseを付けずに try Number.From([金額]) とだけ書いた場合、返る値の説明として正しいものはどれですか?",
          c: ["HasErrorと、エラーがなければValue、エラーがあればErrorというフィールドを持つレコードが返る", "エラーの有無にかかわらず、常に変換後の数値がそのまま返る", "エラーが発生した場合は必ずnullという単純な値が返る", "常にtrueかfalseという論理値だけが返る"],
          a: 0,
          e: "otherwiseを省略したtry式は、HasErrorフィールドで成否を示し、成功時はValue、失敗時はErrorフィールドに詳細情報を持つレコードを返します。Errorの中にはさらにReason・Message・Detailが入っています。"
        },
        {
          type: "mc",
          q: "try式が返すエラーレコードの Message フィールドに入っている情報として正しいものはどれですか?",
          c: ["エラーの内容を人が読める形で説明した文字列", "エラーが発生した行番号を表す数値", "エラーが発生した日時を表すdatetime値", "エラーを修正するためのM式のサンプルコード"],
          a: 0,
          e: "Error.Reason/Message/Detailのうち、Messageはエラーの内容を人が読める形で説明した文字列です。行番号や日時、修正コードは自動的には含まれず、必要なら別途自分で記録する必要があります。"
        },
        {
          type: "mc",
          q: "try...otherwiseによるエラー処理についての説明として正しいものはどれですか?",
          c: ["try...otherwiseはエラー時の代替値を用意するだけで元データの誤りそのものは修正されないため、重要な指標では別途エラー件数を数えるなどの検証が必要になる", "try...otherwiseを使えば、変換に失敗した元データの値も自動的に正しい値へ修正される", "try...otherwiseを一度使うと、以降そのクエリのすべての更新が必ず停止するようになる", "try...otherwiseはエラーの有無にかかわらず警告や通知を一切出せないため、実務では絶対に使うべきではない"],
          a: 0,
          e: "try...otherwiseはあくまで評価結果の代替値を返す仕組みで、ソース側の誤ったデータそのものを修正するわけではありません。件数集計や品質ゲートと組み合わせて初めて、欠損に気づける設計になります。"
        },
        {
          type: "fill",
          q: "「変換後」テーブルから、列「金額数値」にエラーが含まれる行を取り除くM式の空欄を埋めてください。",
          lang: "m",
          code: "Table.RemoveRowsWithErrors(変換後, {【0】})",
          bank: ["\"金額数値\"", "\"金額\"", "true", "Error.Record"],
          answers: [0],
          e: "Table.RemoveRowsWithErrorsの第2引数には、エラーの有無を判定する対象列名のリストを渡します。ここでは変換後にエラーが起きうる「金額数値」列を指定して、エラー行だけを除外します。"
        },
        {
          type: "type",
          q: "テーブル「ソース」の[金額]列をNumber.Fromで数値に変換し、変換に失敗した場合はnullを返す新しい列「金額数値」を追加するM式を、Table.AddColumnとtry...otherwiseを使って書いてください。",
          lang: "m",
          answer: "Table.AddColumn(ソース, \"金額数値\", each try Number.From([金額]) otherwise null)",
          hint: "Table.AddColumn(テーブル, 新列名, each try 変換式 otherwise 代替値)という形です。",
          e: "try Number.From([金額]) otherwise nullは、数値変換が成功すればその値を、失敗すればnullを返します。これをTable.AddColumnのeach部分に渡すことで、行ごとの安全な変換列を追加できます。"
        }
      ]
    },
    {
      title: "パラメータ設計とテンプレート",
      doc: `
<p>「開発環境で作ったレポートを、動作確認が終わったら本番環境のデータベースにつなぎ直したい」「同じ構成のレポートを支店ごとに1つずつ作りたい」——こうした「接続先や条件だけを差し替えて、あとは同じロジックを使い回したい」場面で活躍するのがPower Queryの<b>パラメータ</b>です。パラメータはクエリの一覧に「値」として現れ、他のクエリの中から普通の値のように参照できます。</p>
<p>パラメータを新規作成する画面では、名前・説明に加えて<b>型</b>(テキスト・小数点数・整数・日付・日付時刻・真偽値・任意の型など)を指定し、さらに「候補となる値」を<b>任意の値</b>・<b>値の一覧</b>・<b>クエリ</b>のいずれかから選べます。「値の一覧」を選ぶと、テンプレートを使う人がドロップダウンから決められた候補だけを選べるようになり、想定外の文字列が入力されるミスを防げます。接続先サーバー名を切り替えるような用途では、当然ながら<b>テキスト型</b>を選ぶのが適切です。</p>
<pre>// パラメータ「環境」(テキスト型、値の一覧: "本番","テスト"、現在値="本番")を使い、
// 接続先サーバーを切り替える
Sql.Database(
    if 環境 = "本番" then "prod-server.database.windows.net" else "test-server.database.windows.net",
    "SalesDB"
)</pre>
<p>パラメータを使ったレポートは、「ファイル」→「エクスポート」から<b>Power BIテンプレート(.pbit)</b>として保存できます。.pbitファイルにはレイアウト・クエリ定義・パラメータの定義は保存されますが、データ本体そのものは含まれません。テンプレートを開くとまずパラメータの入力を求められ、値を確定してから初めてデータの読み込みが始まるため、同じ構成のレポートを支店・年度・環境ごとに量産する土台として便利です。</p>
<p>一方で注意したいのが、ネイティブなデータベースクエリを実行しようとしたときに表示される確認ダイアログの落とし穴です。パラメータで組み立てたSQL文などを初めて実行すると、Power Queryは「このクエリを実行してよいか」の確認を求めてきますが、ここで内容を確認せずに「常に許可」に類するチェックを入れてしまうと、以後は同じ確認なしにクエリが自動実行されるようになります。テンプレートを配布した相手の環境でパラメータの値が変わっていても警告なく実行されてしまうため、他人に配るテンプレートほど、この種の確認を安易に無効化しないことが大切です。</p>
<p class="tip">💡 実務メモ: パラメータの「現在値」はPower BI Desktop上での動作確認用の値にすぎず、Power BI Serviceに発行した後は「セマンティックモデルの設定」画面から値を上書きできます(Pro/PPU以上が必要な場合あり)。開発中の値のまま発行して本番データを壊さないよう、発行前に必ず値を見直しましょう。</p>`,
      exercises: [
        {
          type: "mc",
          q: "パラメータの新規作成画面で「候補となる値」に『値の一覧』を選ぶことの意味として正しいものはどれですか?",
          c: ["パラメータに指定した候補値だけをドロップダウンから選ばせ、想定外の値が入力されるミスを防ぐ", "パラメータの型を自動的にリスト型(list)に変える", "パラメータをテーブルとして扱えるようにし、複数行の値を一度に渡せるようにする", "常にWeb.Contentsで最新の候補値を取得し直すようになる"],
          a: 0,
          e: "「値の一覧」を選ぶと、パラメータの値として使える候補をあらかじめ列挙でき、利用者はその中からしか選べなくなります。テンプレート利用者が想定外の文字列を入力してエラーになるのを防ぐ効果があります。"
        },
        {
          type: "mc",
          q: "接続先のサーバー名を切り替えるためのパラメーターに設定する型として最も適切なものはどれですか?",
          c: ["テキスト型(Text)", "真偽値型(True/False)", "整数型(Whole Number)", "期間型(Duration)"],
          a: 0,
          e: "サーバー名は文字列なので、パラメータの型はテキスト型が適切です。真偽値型や整数型では \"prod-server.database.windows.net\" のような値を保持できません。"
        },
        {
          type: "mc",
          q: "Power BIテンプレート(.pbit)についての説明として正しいものはどれですか?",
          c: ["レポートのレイアウト・クエリ定義・パラメータ定義を保存するがデータ本体は含まず、開くときにパラメータの入力が求められる", ".pbitはデータ本体も含めて完全に複製したファイルであり、.pbixと全く同じ動作をする", ".pbitファイルはPower BI Serviceにのみアップロードでき、Desktopでは開けない", "パラメータを1つも定義していないレポートはテンプレート化できない"],
          a: 0,
          e: ".pbitはレポートの構造とクエリ・パラメータの定義だけを保存し、データそのものは含みません。開くとパラメータの入力を求められてから読み込みが始まるため、同じ構成のレポートを量産する土台に向いています。"
        },
        {
          type: "mc",
          q: "ネイティブなデータベースクエリの実行時に表示される確認ダイアログで、内容を確認せずに「常に許可」に類するチェックを入れてしまうことの問題点として正しいものはどれですか?",
          c: ["以後そのクエリが未確認のまま自動実行されるようになり、テンプレートを配布した相手の環境でも意図しないクエリが警告なく走るリスクがある", "チェックを入れると二度とパラメータの値を変更できなくなる", "チェックを入れるとクエリフォールディングが強制的に無効化される", "このチェックは表示上の設定にすぎず、実際の更新動作には一切影響しない"],
          a: 0,
          e: "確認を無効化すると、以後は内容を見ずにクエリが自動実行されるようになります。パラメータで組み立てたクエリを他人に配るテンプレートでは、相手の環境で意図しない実行が警告なく起きるおそれがあるため注意が必要です。"
        },
        {
          type: "fill",
          q: "テキスト型パラメータ「環境」の値に応じて接続先サーバーを切り替えるM式の空欄を埋めてください。",
          lang: "m",
          code: "Sql.Database(if 環境 = \"本番\" then 【0】 else 【1】, \"SalesDB\")",
          bank: ["\"prod-server.database.windows.net\"", "\"test-server.database.windows.net\"", "環境", "Sql.Databases"],
          answers: [0, 1],
          e: "if式でパラメータ「環境」の値を判定し、\"本番\"ならprod-server、それ以外ならtest-serverの接続文字列を返すようにすると、パラメータの値を変えるだけで接続先を切り替えられます。"
        },
        {
          type: "type",
          q: "数値型パラメータ「対象年」を使い、テーブル「ソース」から[年]列が対象年と一致する行だけを残すM式を、Table.SelectRowsを使って書いてください。",
          lang: "m",
          answer: "Table.SelectRows(ソース, each [年] = 対象年)",
          hint: "Table.SelectRows(テーブル, each 条件式)の条件式の中で、パラメータ名をそのまま値として比較に使えます。",
          e: "パラメータはクエリの一覧上では普通の値として扱われるため、each [年] = 対象年のように他の値と同じ感覚で条件式に組み込めます。対象年の値を変えるだけで抽出条件を切り替えられます。"
        }
      ]
    },
    {
      title: "増分更新とデータフロー",
      doc: `
<p>売上テーブルが何年分も積み上がって数千万行になると、毎回全件を洗い替えする更新は時間がかかりすぎて現実的ではなくなります。過去の確定した期間はそのまま保持し、直近の変更が起きやすい期間だけを再取得したい——この要望に応えるのが<b>増分更新(Incremental Refresh)</b>です。増分更新を設定するには、まず<b>RangeStart</b>と<b>RangeEnd</b>という名前の2つのパラメータを用意し、フィルターのステップでこの2つを使って対象期間を絞り込みます。</p>
<p>ここで見落としやすいのが、RangeStartとRangeEndは<b>必ず日付と時刻(datetime)型</b>で定義しなければならないというルールです。フィルター対象の列が時刻を持たないDate型であっても、この2つのパラメータ自体はdatetime型のままにし、比較する側の列を<code>Date.From(...)</code>で型を合わせるという工夫が必要になります。</p>
<pre>// RangeStart・RangeEndはdatetime型のパラメータ。
// [日付]列がDate型の場合はDate.Fromで型を合わせてから比較する
Table.SelectRows(ソース, each [日付] >= Date.From(RangeStart) and [日付] &lt; Date.From(RangeEnd))</pre>
<p>フィルターのステップを用意したら、クエリを右クリックして「増分更新」の設定画面を開き、<b>保持期間</b>(例: 過去5年分をアーカイブとして保持する)と<b>更新期間</b>(例: 直近10日分だけを毎回再取得する)を指定します。保持期間は更新期間以上の長さに設定するのが一般的で、古いデータは触らずに、変更が起こりうる直近の期間だけを洗い替えることで更新時間を大幅に短縮できます。さらに「完全な期間のみ更新する」というオプションを有効にすると、月の途中など締まっていない期間を更新対象から除外し、確定した期間だけを再取得するようになるため、日をまたいだ更新でデータが重複したり欠けたりする事故を防げます。</p>
<p>もう1つ、大規模データを扱う組織で押さえておきたいのが<b>データフロー(Dataflow Gen2)</b>という考え方です。個々のレポートやセマンティックモデルの中に同じPower Queryの変換ロジックを何度も作り込むのではなく、変換処理そのものを独立した部品として保存し、複数のレポートやモデルから共有・再利用できるようにする仕組みです。データ準備の担当者がデータフロー側で「きれいなテーブル」を1か所整備しておけば、各レポート作成者はそれを読み込むだけでよくなり、変換ロジックの重複や食い違いを防げます。</p>
<p class="tip">💡 実務メモ: 増分更新はDesktopのプレビュー環境では小さいサンプル期間で動作確認し、実際の保持期間・更新期間の効果はPower BI Serviceに発行して初めて実感できます。RangeStart/RangeEndの型を間違えると設定画面自体が正しく機能しないため、まず2つのパラメータの型がdatetimeになっているかを最初に確認しましょう。</p>`,
      exercises: [
        {
          type: "mc",
          q: "増分更新で使うRangeStart・RangeEndパラメータの型として必須なのはどれですか?",
          c: ["日付と時刻(datetime)型(フィルター対象の列がDate型の場合でも、パラメータ自体はdatetime型で定義する)", "テキスト型(Text)", "整数型(Whole Number)", "真偽値型(True/False)"],
          a: 0,
          e: "RangeStart・RangeEndは必ずdatetime型で定義する必要があります。フィルター対象の列がDate型の場合は、パラメータ側ではなく比較する列側をDate.Fromなどで型変換して合わせます。"
        },
        {
          type: "mc",
          q: "増分更新の設定における「保持期間」と「更新期間」の関係として適切なものはどれですか?",
          c: ["保持期間(アーカイブする範囲)は更新期間(実際に再取得する範囲)以上の長さに設定するのが一般的で、例えば過去5年分を保持しつつ直近10日分だけを更新する、といった構成にする", "更新期間は必ず保持期間より長く設定しなければならない", "保持期間と更新期間は常に同じ長さでなければ増分更新は機能しない", "増分更新では保持期間の設定は不要で、更新期間だけを指定すればよい"],
          a: 0,
          e: "保持期間は過去データをどこまでアーカイブとして残すか、更新期間はそのうちどこまでを毎回洗い替えるかを表します。保持期間を更新期間以上にすることで、古いデータは触らずに直近だけを効率よく再取得できます。"
        },
        {
          type: "mc",
          q: "データフロー(Dataflow Gen2)の役割として正しいものはどれですか?",
          c: ["Power Queryによる変換ロジックをモデリングから切り離して独立に保存し、複数のレポートやセマンティックモデルから共有・再利用できるようにする仕組み", "複数人が1つのレポート(ビジュアル)を同時に共同編集するための機能", "DAXメジャーの定義を複数のセマンティックモデル間で共有するための機能", "増分更新を無効化し、常に全件更新に切り替えるための設定"],
          a: 0,
          e: "データフローは変換処理(Power Queryのロジック)を独立した部品として保存し、複数のレポートやモデルから読み込んで再利用できるようにする仕組みです。変換ロジックの重複や食い違いを防げます。"
        },
        {
          type: "mc",
          q: "増分更新の「完全な期間のみ更新する」オプションを有効にすることの意味として正しいものはどれですか?",
          c: ["月の途中などまだ終わっていない期間を更新対象から除外し、締まった(完結した)期間だけを再取得することで、データの重複や欠落を防ぐ", "更新が一度失敗すると、次回から自動的に全期間を再取得するようになる", "保持期間の設定をすべて無視し、常に全件を「完全に」更新する", "レポートの表示に関する設定であり、増分更新の動作そのものには影響しない"],
          a: 0,
          e: "このオプションを有効にすると、まだ終わっていない期間(例えば今月分)は更新対象から外れ、確定済みの期間だけが再取得されます。日をまたぐタイミングでの更新によるデータの重複・欠落を防ぐ効果があります。"
        },
        {
          type: "fill",
          q: "テーブル「ソース」の[日付]列(Date型)が、datetime型のパラメータRangeStart以上RangeEnd未満の範囲に入る行だけを残す、増分更新用のフィルターM式の空欄を埋めてください。",
          lang: "m",
          code: "Table.SelectRows(ソース, each [日付] >= 【0】(RangeStart) and [日付] < 【1】(RangeEnd))",
          bank: ["Date.From", "DateTime.From", "Number.From", "Text.From"],
          answers: [0, 0],
          e: "RangeStart・RangeEndはdatetime型ですが、[日付]列はDate型なので、Date.Fromで型を合わせてから比較します。同じDate.Fromを開始側・終了側の両方で使います。"
        },
        {
          type: "type",
          q: "テーブル「ソース」の[日付]列(Date型)が、datetime型のパラメータRangeStart以上RangeEnd未満の範囲に入る行だけを残す、増分更新用のフィルターM式を、Date.Fromで型を合わせながらTable.SelectRowsを使って書いてください。",
          lang: "m",
          answer: "Table.SelectRows(ソース, each [日付] >= Date.From(RangeStart) and [日付] < Date.From(RangeEnd))",
          hint: "[日付]はDate型、RangeStart/RangeEndはdatetime型なので、Date.Fromで型をそろえてから開始側は以上(>=)、終了側は未満(<)で比較します。",
          e: "開始側を以上(>=)、終了側を未満(<)にすることで期間の重複や欠落を防ぐのが増分更新フィルターの定石です。列とパラメータの型が食い違う場合はDate.Fromなどで揃える必要があります。"
        }
      ]
    },
    {
      title: "パフォーマンス戦略",
      doc: `
<p>同じPower Queryのロジックでも、ステップの組み方1つで更新時間が大きく変わることがあります。データ量が数百行の演習環境ではほとんど気にならない差でも、実務で数百万行のテーブルを毎晩更新するとなると、その差が数分・数十分の違いになって現れます。ここでは、大規模データでも実用的な速度を保つための代表的な戦略を整理します。</p>
<p>最も基本的で効果が大きいのが<b>早期の列削減</b>です。使わない列は、できるだけ最初のステップに近いところでTable.SelectColumnsによって落としてしまいます。フォールディングが効くデータソースであれば、ソース側のSELECT文自体が必要な列だけを取得するように変換されるため、以降のすべてのステップが扱うデータ量そのものが減り、転送量・メモリ使用量の両方が軽くなります。</p>
<pre>// 使わない列は早い段階で落とす。フォールディングが効けば
// ソース側のSELECT文自体が必要な列だけを取得するようになる
Table.SelectColumns(Source, {"商品ID", "数量", "金額"})

// 何度も参照される小さなテーブルをメモリに固定する
商品_buffer = Table.Buffer(商品)</pre>
<p><b>Table.Buffer</b>は、その時点のテーブルを強制的にメモリ上に固定する関数です。同じテーブルを複数の場所から繰り返し参照するときに、参照のたびに元のステップが再評価されるのを防げる場合があり、うまく使えば体感速度が改善します。ただしTable.Bufferを使った時点でそれ以降のステップはソース側へのフォールディングが効かなくなり、テーブルが大きい場合はメモリを大量に消費してかえって遅くなることもあります。小さなマスタテーブルには効果的でも、巨大なファクトテーブルに安易に使うのは危険な、まさに諸刃の剣です。</p>
<p>実際にどこで時間がかかっているかを特定するには、Power Queryエディターの「クエリ診断」機能を使います。「診断の開始」を押してから更新やプレビュー更新を実行し、「診断の停止」を押すと、各ステップの実行にかかった時間や、実際にソースへ送信されたネイティブクエリの内容などが記録として残ります。感覚だけで「ここが遅そう」と決めつけず、まずクエリ診断で実測してから対策を打つのが遠回りに見えて確実な近道です。また、「参照(Reference)」で作った派生クエリは参照元のステップを土台にしているため、参照元に重い処理が含まれていると、その派生クエリが読み込まれるたびに参照元のステップも改めて評価されることがあります。複数の派生クエリから共通して参照される重い処理は、Table.Bufferで一度固定しておくことで再評価の回数を抑えられる場合があります。</p>
<p class="tip">💡 実務メモ: 「なんとなく遅い気がする」で最適化に手を付けると、効果のない変更に時間を使いがちです。まずクエリ診断でボトルネックのステップを特定し、そこがフォールディング可能な操作を後回しにしているせいなのか、Table.Bufferが必要な繰り返し参照のせいなのかを切り分けてから手を打ちましょう。</p>`,
      exercises: [
        {
          type: "mc",
          q: "使わない列をできるだけ早い段階(ソースに近いステップ)で削減することの利点として正しいものはどれですか?",
          c: ["フォールディング可能なソースでは、ソース側のクエリ自体が必要な列だけを取得するようになり、以降のステップで扱うデータ量そのものが減る", "列削減はどのタイミングで行っても結果にもパフォーマンスにも一切差が出ない", "列を減らすとクエリフォールディングは必ず無効になってしまう", "列削減はプレビュー画面の表示速度にのみ影響し、実際の更新時間には無関係である"],
          a: 0,
          e: "フォールディングが効くソースでは、早い段階での列選択がソース側のSELECT句に反映され、それ以降のすべてのステップが扱うデータ量自体を減らせます。後段で列を削っても、それまでの転送・処理量は減りません。"
        },
        {
          type: "mc",
          q: "Table.Bufferの特性についての説明として正しいものはどれですか?",
          c: ["その時点のテーブルをメモリに固定して再評価を避けられる一方、それ以降のステップはソース側へのフォールディングが効かなくなり、大きなテーブルではメモリを圧迫しかねない諸刃の剣である", "Table.Bufferは常に更新を高速化する、デメリットのない万能の最適化手段である", "Table.Bufferを使うとクエリフォールディングがそれ以降のステップでもより効きやすくなる", "Table.Bufferは列のデータ型を自動的に最適な型へ変換する関数である"],
          a: 0,
          e: "Table.Bufferは繰り返し参照によるステップの再評価を防げる一方で、それ以降のフォールディングを打ち切り、大きなテーブルではメモリを圧迫します。効果とリスクの両面を理解したうえで、限定的な場面で使うべき機能です。"
        },
        {
          type: "mc",
          q: "Power Queryの「クエリ診断(Query Diagnostics)」で確認できることとして正しいものはどれですか?",
          c: ["各ステップの実行にかかった時間や、実際にソースへ送信されたネイティブクエリなど、更新処理の内訳を記録して確認できる", "モデル内のDAXメジャー同士の依存関係を可視化できる", "レポート上の各ビジュアルの描画速度を測定できる", "セマンティックモデルのVertiPaq圧縮後の列サイズを表示できる"],
          a: 0,
          e: "クエリ診断は「診断の開始」から「診断の停止」までの間の更新・プレビュー更新について、各ステップの所要時間や送信されたネイティブクエリなどを記録します。感覚ではなく実測でボトルネックを特定するための機能です。"
        },
        {
          type: "mc",
          q: "「参照(Reference)」で作った派生クエリの評価についての説明として正しいものはどれですか?",
          c: ["参照元クエリに重い処理が含まれる場合、派生クエリが読み込まれるたびに参照元のステップも改めて評価されることがあり、複数の派生クエリから使われる重い処理はTable.Bufferで固定して再評価を抑えられる場合がある", "参照して作ったクエリは参照元と完全に独立しており、参照元を変更しても派生クエリの結果には一切影響しない", "「参照」は「複製」と実行時の挙動もまったく同じであり、パフォーマンス上の違いは存在しない", "参照して作ったクエリの結果は常に自動的にキャッシュされるため、参照元が何回評価されるかを気にする必要はない"],
          a: 0,
          e: "参照クエリは参照元のステップを土台に評価されるため、参照元が重い処理を含む場合、複数の派生クエリから読み込まれるたびに再評価されることがあります。共通して使われる重い処理はTable.Bufferで固定すると再評価を抑えられる場合があります。"
        },
        {
          type: "fill",
          q: "テーブル「Source」から「商品ID」「数量」「金額」の3列だけを早い段階で選び出すM式の空欄を埋めてください。",
          lang: "m",
          code: "Table.SelectColumns(Source, {\"商品ID\", 【0】, \"金額\"})",
          bank: ["\"商品ID\"", "\"数量\"", "\"顧客ID\"", "\"カテゴリ\""],
          answers: [1],
          e: "Table.SelectColumnsの第2引数に残したい列名のリストを渡します。ここでは「商品ID」「数量」「金額」の3列だけを選び、不要な列を早い段階で落としています。空欄には「数量」が入ります。"
        },
        {
          type: "type",
          q: "テーブル「商品」を、以降のステップで繰り返し参照されても再評価されないようにメモリに固定するM式を、Table.Bufferを使って書いてください。",
          lang: "m",
          answer: "Table.Buffer(商品)",
          hint: "Table.Buffer(テーブル)という1引数だけの関数です。",
          e: "Table.Buffer(テーブル)はその時点のテーブルをメモリに固定します。複数の場所から繰り返し参照される小さなマスタテーブル(この例では「商品」テーブル)に使うと、参照のたびの再評価を防げる場合があります。"
        }
      ]
    }
  ]
});
