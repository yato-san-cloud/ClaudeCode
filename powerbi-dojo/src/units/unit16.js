window.DOJO_UNITS = window.DOJO_UNITS || [];
window.DOJO_UNITS.push({
  id: "unit16",
  no: 6,
  track: "advanced",
  icon: "⚙️",
  name: "M応用 I: 変換の匠",
  color: "#7a5230",
  desc: "カスタム関数・ピボット/アンピボット・マージ完全版・グループ化応用・List.Generate/Accumulateまで、「販売サンプル」モデルでPower Queryの変換力を一段引き上げる",
  lessons: [
    {
      title: "カスタム関数と再利用",
      doc: `
<p>営業企画チームから「割引後の単価を計算するロジックを、3つの違うクエリで同じように使いたい」と相談されたとします。同じ計算式を3か所にコピペすれば動くには動きますが、後で税率や丸め方を直すたびに3か所を直し忘れるリスクを抱えることになります。M言語では<b>クエリそのものを関数として定義</b>できるので、こうした「同じロジックの使い回し」を1か所に集約できます。</p>
<p>クエリの詳細エディターに<code>(引数名 as 型) as 戻り値の型 =&gt; 式</code>という形を書くと、そのクエリ自体が<b>カスタム関数</b>になります。呼び出す側は<code>関数名(実引数)</code>と書くだけで、他の組み込み関数と同じ感覚で使えます。引数の前に<b>optional</b>を付けると省略可能な引数になり、呼び出し側が値を渡さなかったときは<code>null</code>として扱われます。関数の呼び出し画面に説明文やサンプル値を表示する「関数ドキュメント」を付けたい場合は、<code>Documentation.Name</code>や<code>Documentation.LongDescription</code>、<code>Documentation.Examples</code>といったフィールドを持つmetaレコードを<code>Value.ReplaceType</code>で関数の型に付与する、という応用テクニックが使われます(詳しくは応用編で扱います)。</p>
<pre>// クエリ名: 割引後単価
(単価 as number, optional 割引率 as number) as number =>
    let
        率 = if 割引率 = null then 0 else 割引率,
        結果 = 単価 * (1 - 率)
    in
        結果

割引後単価(1000, 0.1)   // 900
割引後単価(1000)        // 1000(optionalなので割引率省略時はnull扱い)</pre>
<p>定義したカスタム関数を1件ずつ呼ぶだけでなく、テーブル全体の各行に適用したいことも多くあります。その場合は<b>Table.AddColumn(テーブル, 新列名, each 関数(...))</b>の第3引数で、行ごとの値を渡しながらカスタム関数を呼び出します。<code>each</code>はテーブルの各行を渡す構文糖衣なので、<code>[単価]</code>のように角かっこで現在の行の列にアクセスできます。こうすることで「1件分の計算ロジック」と「テーブル全体への適用」がきれいに分離され、ロジックだけを直せば全体に反映されます。</p>
<p class="tip">💡 実務メモ: カスタム関数はクエリの一覧では「関数」アイコンで表示され、そのままでは読み込み対象になりません(呼び出し元のクエリから参照されるだけの部品です)。誤って読み込みを有効にしたままレポートに反映しようとするとエラーになるので注意しましょう。</p>`,
      exercises: [
        {
          type: "mc",
          q: "M言語で「クエリをカスタム関数として定義する」ための書き方として正しいものはどれですか?",
          c: ["詳細エディターに (引数 as 型) as 戻り値の型 => 式 という形式でクエリを書く", "クエリ名の先頭に \"fn_\" と付ければ自動的に関数として扱われる", "Table.AddColumnの中に直接すべての計算式を書けば、それがカスタム関数になる", "let式の中でinの代わりにreturnキーワードを使う"],
          a: 0,
          e: "M言語では (引数 as 型, ...) as 戻り値の型 => 式 という形の値そのものがクエリとして定義されると、そのクエリ自体が呼び出し可能な関数になります。特別なキーワードや命名規則は不要です。"
        },
        {
          type: "mc",
          q: "次の関数定義で 割引後単価(1000) のように第2引数を省略して呼び出した場合の挙動として正しいものはどれですか?",
          code: "(単価 as number, optional 割引率 as number) as number =>\n    let\n        率 = if 割引率 = null then 0 else 割引率,\n        結果 = 単価 * (1 - 率)\n    in\n        結果",
          lang: "m",
          c: ["割引率は optional なので省略時は null として扱われ、この定義では率=0となり結果は単価そのままになる", "省略した引数は自動的に0として扱われるため、この定義に関わらず率は常に0になる", "optional な引数を省略するとエラーになり、呼び出しに失敗する", "省略された引数には自動的に1が代入される"],
          a: 0,
          e: "optionalを付けた引数は呼び出し時に省略可能で、省略すると値はnullになります。この関数はnullかどうかをifで判定して率を0に読み替えているため、割引率なしの単価がそのまま返ります。"
        },
        {
          type: "mc",
          q: "テーブル「売上」の各行に対してカスタム関数「割引後単価」を[単価]列を渡して適用し、新しい列を追加したいとき、最も適切な書き方はどれですか?",
          c: ["Table.AddColumn(売上, \"割引後単価\", each 割引後単価([単価]))", "Table.AddColumn(売上, \"割引後単価\", 割引後単価(売上[単価]))", "Table.TransformColumnTypes(売上, {{\"単価\", 割引後単価}})", "割引後単価(売上, \"単価\") と直接テーブルごと渡す"],
          a: 0,
          e: "Table.AddColumnの第3引数にeachで始まる式を渡すと、テーブルの各行に対してその式が評価されます。行ごとに[単価]を関数に渡すことで、1件分のロジックをテーブル全体に適用できます。"
        },
        {
          type: "fill",
          q: "単価に対して割引率(0〜1の小数、省略可)を適用した割引後単価を返すカスタム関数の空欄を埋めてください。",
          lang: "m",
          code: "(単価 as number, 【0】 割引率 as number) as number =>\n    let\n        率 = if 割引率 = null then 0 else 割引率\n    in\n        単価 * (1 - 【1】)",
          bank: ["optional", "率", "required", "割引率"],
          answers: [0, 1],
          e: "省略可能な引数にはoptionalを付けます。計算式では、nullを0に読み替えた変数「率」を使うことで、割引率を渡さなかった場合でも単価がそのまま返る安全な作りになります。"
        },
        {
          type: "type",
          q: "テーブル「売上」の各行に対して、カスタム関数「割引後単価」を[単価]列の値を渡して適用し、新しい列「割引後単価」を追加するM式をTable.AddColumnで書いてください。",
          lang: "m",
          answer: "Table.AddColumn(売上, \"割引後単価\", each 割引後単価([単価]))",
          hint: "Table.AddColumn(テーブル, 新列名, each カスタム関数([参照する列]))という形です。",
          e: "Table.AddColumnのeach部分で行ごとの[単価]をカスタム関数に渡すと、1件分のロジックをテーブル全体に一括適用できます。ロジックの変更は関数定義側だけで済むのが再利用の利点です。"
        }
      ]
    },
    {
      title: "ピボットとアンピボット",
      doc: `
<p>経理部から届く月次売上のExcelファイルを開いたら、「1月」「2月」「3月」…と月ごとの列が横に並んだ、いわゆる<b>横持ち(ワイド形式)</b>の表だった、という経験は多いはずです。人が目で見る分には分かりやすい形ですが、Power BIでスライサーや軸に「月」を使いたい場合、この形のままでは月の値を1つの列として扱えず不便です。こうした形を、1行が1つの事実を表す<b>縦持ち(ロング形式)</b>に変換する操作が<b>アンピボット(Unpivot)</b>です。</p>
<p>UIでは列を複数選択して右クリックし「列のピボット解除」を選んだ場合も、選択していない列も含めて「その他の列のピボット解除」を選んだ場合も、生成されるのは<b>Table.UnpivotOtherColumns</b>です(前者は「選択列以外」がその時点でたまたま無いだけで、内部的には同じ関数が使われます)。<b>Table.Unpivot</b>が生成されるのは、列を選択したうえでさらに「選択した列のみをピボット解除」を選んだときだけで、この場合は選択しなかった列も含めて展開対象になる点に注意が必要です。実務では新しい月の列が追加されるたびに選択列を更新するのは手間なので、「商品IDなど固定したい列だけ」を指定できるTable.UnpivotOtherColumnsのほうがよく使われます。逆に、縦持ちのデータをある列の値ごとに横へ展開したいときは<b>Table.Pivot</b>を使います。</p>
<pre>// 横持ち→縦持ち(商品IDは固定し、それ以外の列(1月・2月・3月…)を展開)
Table.UnpivotOtherColumns(ソース, {"商品ID"}, "年月", "数量")

// 縦持ち→横持ち(年月列の値ごとに列を作り、数量列の値を集計して展開)
Table.Pivot(ソース, List.Distinct(ソース[年月]), "年月", "数量", List.Sum)</pre>
<p>Table.UnpivotOtherColumnsの引数は「固定する列のリスト」「展開後にできる属性名の列名」「展開後にできる値の列名」の順です。逆方向のTable.Pivotでは、「どの列の値を新しい列名にするか」「新しい列名になる値の一覧」「集計対象の値が入っている列」「複数行が同じ組み合わせになったときの集計関数」を指定します。集計関数を省略すると複数値が衝突した場合にエラーになりやすいため、<code>List.Sum</code>や<code>List.Max</code>など目的に合った関数を明示するのが安全です。</p>
<p class="tip">💡 実務メモ: 「月が列に並んだExcel」を読み込む定番シナリオでは、まずTable.UnpivotOtherColumnsで縦持ちに正規化し、生成された「属性」列を「年月」に、「値」列を「数量」にTable.RenameColumnsで改名する流れが型になります。縦持ちにしておけば、後からDAXの日付テーブルとリレーションを組むのも容易になります。</p>`,
      exercises: [
        {
          type: "mc",
          q: "「商品ID」「1月」「2月」「3月」という横持ちの表を、商品IDを固定したまま月ごとの列を1つの「年月」列と「数量」列に変換したいとき、最も適した関数はどれですか?",
          c: ["Table.UnpivotOtherColumns(ソース, {\"商品ID\"}, \"年月\", \"数量\")", "Table.Pivot(ソース, {\"1月\", \"2月\", \"3月\"}, \"年月\", \"数量\", List.Sum)", "Table.TransposeColumns(ソース, {\"商品ID\"})", "Table.SelectColumns(ソース, {\"商品ID\", \"1月\", \"2月\", \"3月\"})"],
          a: 0,
          e: "Table.UnpivotOtherColumnsは指定した固定列以外のすべての列を縦持ちに展開する関数です。月の列が増減しても固定列(商品ID)の指定だけで済むため、横持ち→縦持ちの定番パターンとして使われます。"
        },
        {
          type: "mc",
          q: "Table.UnpivotOtherColumns(ソース, {\"商品ID\"}, \"年月\", \"数量\") を実行した結果、生成される列の説明として正しいものはどれですか?",
          code: "Table.UnpivotOtherColumns(ソース, {\"商品ID\"}, \"年月\", \"数量\")",
          lang: "m",
          c: ["「商品ID」列はそのまま残り、それ以外だった列名(1月・2月など)が「年月」列の値に、元のセルの値が「数量」列の値になる", "「商品ID」列が削除され、「年月」と「数量」という2列だけの表になる", "「1月」「2月」「3月」の列名がそのまま残り、「商品ID」だけが「年月」列に変換される", "何も変化せず、列名が「年月」「数量」に一括リネームされるだけである"],
          a: 0,
          e: "アンピボットは固定列(商品ID)を残したまま、それ以外の各列を「属性名(元の列名)」と「値(元のセルの値)」のペアに縦展開します。第3・第4引数はその2つの新しい列名を指定しています。"
        },
        {
          type: "mc",
          q: "Table.Unpivot と Table.UnpivotOtherColumns の実務上の使い分けとして正しいものはどれですか?",
          c: ["Table.Unpivotは展開したい列を明示的に列挙するのに対し、Table.UnpivotOtherColumnsは固定したい列だけを指定し残りをすべて展開するため、列が増減するデータでは後者のほうが保守しやすい", "Table.Unpivotは数値専用、Table.UnpivotOtherColumnsはテキスト専用の関数である", "両者はまったく同じ結果を返す完全な同義語であり、使い分けの意味はない", "Table.UnpivotOtherColumnsはテーブルの行数を変えず列数だけを変える関数である"],
          a: 0,
          e: "Table.Unpivotは展開対象の列を列挙する方式のため、新しい月の列が増えるたびに式を修正する必要があります。固定列だけを指定するTable.UnpivotOtherColumnsは列の増減に強く、月次で列が増える横持ちデータの定番対処です。"
        },
        {
          type: "fill",
          q: "縦持ちの表(年月・数量の列を持つ)を、年月ごとの列に展開して横持ちに戻すM式の空欄を埋めてください。",
          lang: "m",
          code: "Table.Pivot(ソース, List.Distinct(ソース[年月]), \"年月\", \"数量\", 【0】)",
          bank: ["List.Sum", "List.Distinct", "Table.Group", "List.Count"],
          answers: [0],
          e: "Table.Pivotの最後の引数には、同じ新しい列に複数の値が集まったときにどう集計するかを指定します。数量を合計したい場合はList.Sumを渡します。"
        },
        {
          type: "type",
          q: "テーブル「ソース」を、「商品ID」列を固定して、それ以外の列を「年月」列と「数量」列に展開(アンピボット)するM式を、Table.UnpivotOtherColumnsを使って書いてください。",
          lang: "m",
          answer: "Table.UnpivotOtherColumns(ソース, {\"商品ID\"}, \"年月\", \"数量\")",
          hint: "第1引数はテーブル、第2引数は固定する列名のリスト、第3・第4引数は新しくできる属性名列と値列の名前です。",
          e: "Table.UnpivotOtherColumnsは固定列以外のすべての列を「属性名(年月)」と「値(数量)」のペアに縦展開する関数です。月ごとに列が並ぶExcelを正規化する定番の1行です。"
        }
      ]
    },
    {
      title: "マージ完全版",
      doc: `
<p>「売上テーブルに商品カテゴリを付け足したい」「今月新規に登録された顧客だけを抽出したい」——どちらも2つのテーブルを何らかのキーで突き合わせる操作で、Power Queryではこれを<b>マージ(Merge)</b>と呼びます。SQLのJOINに慣れていれば発想は同じですが、Power Queryでは<b>JoinKind</b>という列挙値でどの行を残すかを指定します。</p>
<p>代表的なJoinKindは6種類です。<b>Inner</b>は両方のテーブルにキーが一致する行だけを残し、<b>LeftOuter</b>は左側(第1引数)のテーブルを全件残して右側を一致した分だけ付加します(一致しない行の右側列はnull)。<b>RightOuter</b>はその逆、<b>FullOuter</b>は両方の全件を残します。<b>LeftAnti</b>は「左側にはあるが右側には一致しない行だけ」、<b>RightAnti</b>はその逆で、どちらも<b>差集合(存在しない行の抽出)</b>によく使われます。「今月の新規顧客」のような抽出はLeftAntiの典型的な使いどころです。</p>
<pre>Table.NestedJoin(売上, {"顧客ID"}, 顧客, {"顧客ID"}, "顧客詳細", JoinKind.LeftOuter)
Table.NestedJoin(売上, {"商品ID", "年月"}, 実績, {"商品ID", "年月"}, "実績詳細", JoinKind.Inner)
Table.NestedJoin(今月顧客, {"顧客ID"}, 先月顧客, {"顧客ID"}, "一致", JoinKind.LeftAnti)</pre>
<p>Table.NestedJoinの第2・第4引数はキー列のリストで、複数列を渡せば<b>複合キー</b>での一致判定になります(すべてのキー列が一致した行だけがマッチとみなされます)。マージ結果はいったん「入れ子(ネスト)テーブル」の列として追加されるので、続けてTable.ExpandTableColumnでその中の必要な列だけを展開します。あいまい一致(Fuzzy Matching)というオプションを使うと、表記ゆれ("株式会社ABC"と"ABC株式会社"など)がある文字列同士でも類似度に基づいて一致させられますが、意図しない誤マッチを生みやすく処理も重いため、まずはキー列の型・空白・大文字小文字を揃える<b>キー整備</b>を徹底し、あいまい一致は最後の手段として慎重に使うべきです。</p>
<p class="tip">💡 実務メモ: マージがうまくいかないときの原因の大半は「片方はテキスト型、もう片方は数値型」「片方に前後の空白が入っている」「全角/半角が混在している」といったキー列の表記ゆれです。あいまい一致に頼る前に、Table.TransformColumnTypesやText.Trim、Text.Upperでキーの形を揃えられないか確認しましょう。</p>`,
      exercises: [
        {
          type: "mc",
          q: "JoinKind.LeftOuter でテーブルAとテーブルBをマージした場合の結果として正しいものはどれですか?",
          c: ["テーブルAの全行が残り、テーブルBはキーが一致した行だけが付加される(一致しない場合はBの列がnullになる)", "テーブルAとテーブルBの両方でキーが一致した行だけが残る", "テーブルBの全行が残り、テーブルAはキーが一致した行だけが付加される", "テーブルAとテーブルBの全行が、一致の有無にかかわらずそのまま結合される"],
          a: 0,
          e: "LeftOuterは第1引数(左側)のテーブルを全件残す結合で、右側は一致した分だけ列が埋まり、一致しない行の右側列はnullになります。「売上に商品カテゴリを付け足す」のように主テーブルを欠落させたくない場合の定番です。"
        },
        {
          type: "mc",
          q: "「今月の顧客一覧」から「先月の顧客一覧」に存在しない(=今月新規の)顧客だけを抽出したいとき、Table.NestedJoinのJoinKindとして最も適切なものはどれですか?",
          c: ["JoinKind.LeftAnti(左側の今月顧客のうち、右側の先月顧客に一致しない行だけを残す)", "JoinKind.Inner(両方に一致する行だけを残す)", "JoinKind.FullOuter(両方の全行を残す)", "JoinKind.RightOuter(右側の先月顧客を全件残す)"],
          a: 0,
          e: "LeftAntiは左側のテーブルのうち、右側に一致する行が存在しないものだけを残す差集合の結合です。「今月にはいるが先月にはいない」新規顧客の抽出に直接使えます。"
        },
        {
          type: "mc",
          q: "Table.NestedJoin(売上, {\"商品ID\", \"年月\"}, 実績, {\"商品ID\", \"年月\"}, \"実績詳細\", JoinKind.Inner) のキー指定の説明として正しいものはどれですか?",
          code: "Table.NestedJoin(売上, {\"商品ID\", \"年月\"}, 実績, {\"商品ID\", \"年月\"}, \"実績詳細\", JoinKind.Inner)",
          lang: "m",
          c: ["「商品ID」と「年月」の両方が一致した行だけをマッチとみなす複合キーでの結合である", "「商品ID」または「年月」のどちらか一方が一致すればマッチとみなされる", "キーのリストは表示用のラベルにすぎず、実際の一致判定には使われない", "1回のNestedJoinでは複数列をキーに指定できないため、この式はエラーになる"],
          a: 0,
          e: "第2・第4引数にリストで複数の列名を渡すと、それらすべての列が一致した行だけをマッチとみなす複合キー結合になります。商品IDと年月の両方が同じ行だけが実績と紐づきます。"
        },
        {
          type: "fill",
          q: "売上テーブルに顧客テーブルを顧客IDでLeftOuter結合し、「顧客詳細」という列名でネストするM式の空欄を埋めてください。",
          lang: "m",
          code: "Table.NestedJoin(売上, {\"顧客ID\"}, 顧客, {\"顧客ID\"}, 【0】, 【1】)",
          bank: ["\"顧客詳細\"", "JoinKind.LeftOuter", "\"顧客\"", "JoinKind.Inner"],
          answers: [0, 1],
          e: "第5引数はマージ結果を格納する新しい列の名前、第6引数はJoinKindです。左側(売上)を全件残したいのでJoinKind.LeftOuterを指定します。"
        },
        {
          type: "type",
          q: "テーブル「今月顧客」を、テーブル「先月顧客」に対して顧客IDでLeftAnti結合し、「一致」という列名でネストするM式を、Table.NestedJoinを使って書いてください。",
          lang: "m",
          answer: "Table.NestedJoin(今月顧客, {\"顧客ID\"}, 先月顧客, {\"顧客ID\"}, \"一致\", JoinKind.LeftAnti)",
          hint: "Table.NestedJoin(左テーブル, {キー}, 右テーブル, {キー}, 新列名, JoinKind.LeftAnti)という順番です。",
          e: "LeftAntiは左側(今月顧客)のうち右側(先月顧客)に一致しない行だけを残す差集合の結合で、新規顧客の抽出という典型的なシナリオに使われます。"
        }
      ]
    },
    {
      title: "グループ化の応用",
      doc: `
<p>「商品カテゴリごとの売上合計と、そのカテゴリで一番売れた商品を1行にまとめたい」——単純な合計だけならピボットテーブルでも足りますが、「グループの中で条件を満たす行だけを別途取り出す」ような集計になると、Power Queryの<b>Table.Group</b>の柔軟さが効いてきます。以下の例では、販売サンプルモデルの売上テーブル([売上ID][日付][顧客ID][商品ID][数量][金額])に商品テーブル([商品ID][商品名][カテゴリ][単価][原価])をあらかじめマージ・展開し、「カテゴリ」「商品名」列を持たせた状態のクエリを「売上」として扱っています。</p>
<p>Table.Groupの基本形は<code>Table.Group(テーブル, グループ化する列, 集計仕様のリスト)</code>で、集計仕様は<code>{"新列名", each 集計式}</code>という形のリストをさらにリストで囲んで複数指定できます。ここで重要なのが、集計式の中で使える<b>_(アンダースコア)</b>という特殊な変数です。これはそのグループに属する行だけを集めた<b>部分テーブル(すべての行)</b>を表しており、<code>List.Sum(_[金額])</code>のように列を指定して集計するだけでなく、<code>Table.Sort</code>や<code>Table.FirstN</code>と組み合わせてそのグループ内の「トップN」を抽出することもできます。</p>
<pre>Table.Group(売上, {"カテゴリ"}, {
    {"合計金額", each List.Sum(_[金額])},
    {"件数", each Table.RowCount(_)},
    {"最高額商品", each Table.FirstN(Table.Sort(_, {{"金額", Order.Descending}}), 1){0}[商品名]}
})</pre>
<p>1つ目の集計仕様は単純にグループ内の金額を合計するList.Sum、2つ目は件数を数えるTable.RowCount、3つ目がやや高度で、グループ内の部分テーブル<code>_</code>を金額の降順に並べ替え(Table.Sort)、先頭1行だけを取り出し(Table.FirstN)、さらに<code>{0}</code>でリストの先頭要素にアクセスしてから<code>[商品名]</code>で列の値を取り出しています。UI上の「グループ化」ダイアログでも、詳細設定を開いて複数の集計を追加すれば同じ形のコードが生成されるので、まずUIで骨格を作ってから数式バーで<code>_</code>を使った応用集計に書き換えるとつまずきにくくなります。</p>
<p class="tip">💡 実務メモ: Table.Group内の各集計式は互いに独立しており、1つの式の中で別の集計仕様の結果(例えば合計金額)を参照することはできません。「グループ内トップN」のような複雑な集計は、必ず部分テーブル_から直接組み立てる必要があります。</p>`,
      exercises: [
        {
          type: "mc",
          q: "Table.Group(売上, {\"カテゴリ\"}, {{\"合計金額\", each List.Sum(_[金額])}}) における「_」の意味として正しいものはどれですか?",
          code: "Table.Group(売上, {\"カテゴリ\"}, {{\"合計金額\", each List.Sum(_[金額])}})",
          lang: "m",
          c: ["集計対象になっている、そのグループに属する行だけを集めた部分テーブル", "グループ化した列(カテゴリ)の値そのもの", "テーブル全体の行数を表す数値", "常に0を表すプレースホルダーで、実際には使われていない"],
          a: 0,
          e: "Table.Groupの集計式の中の_は、そのグループに属する行だけを集めた部分テーブル(いわゆる「すべての行」)を表します。List.Sum(_[金額])はこの部分テーブルの金額列を合計しています。"
        },
        {
          type: "mc",
          q: "Table.Group内の集計式でグループ内の件数を数えたい場合、最も適切な書き方はどれですか?",
          c: ["each Table.RowCount(_)", "each List.Sum(_)", "each Table.RowCount(売上)", "each _[件数]"],
          a: 0,
          e: "Table.RowCount(_)は部分テーブル_の行数、つまりそのグループに属する行の件数を返します。売上テーブル全体ではなく、そのグループだけの件数を数える点が重要です。"
        },
        {
          type: "mc",
          q: "各カテゴリの中で金額が最も高い1件の商品名を取り出したい場合、部分テーブル_に対して行う操作の順序として正しいものはどれですか?",
          c: ["_を金額の降順にTable.Sortで並べ替え、Table.FirstNで先頭1行を取り出し、{0}[商品名]で商品名の値を取得する", "_をそのままList.Max(_[金額])に渡すだけで商品名が直接得られる", "_をTable.RowCountに渡して件数を数えるだけでよい", "_を並べ替えずにTable.FirstNだけを使えば、最も金額が高い行が自動的に先頭になる"],
          a: 0,
          e: "最大値の行を特定するには、まず並べ替えて目的の行を先頭に持ってくる必要があります。Table.Sortで降順に並べ、Table.FirstNで1行に絞り、{0}でリストの先頭要素(レコード)を取り、[商品名]でその列の値を取り出します。"
        },
        {
          type: "fill",
          q: "「カテゴリ」ごとにグループ化し、グループ内の金額合計と件数を求めるM式の空欄を埋めてください。",
          lang: "m",
          code: "Table.Group(売上, {\"カテゴリ\"}, {\n    {\"合計金額\", each List.Sum(【0】[金額])},\n    {\"件数\", each 【1】(_)}\n})",
          bank: ["_", "Table.RowCount", "売上", "List.Count"],
          answers: [0, 1],
          e: "Table.Groupの集計式では、そのグループに属する部分テーブルを表す_を使って各種の集計(List.SumやTable.RowCount)を行います。件数を数えるにはTable.RowCount(_)を使います。List.Countはリストを数える関数なので、テーブルである_にはそのまま使えません。"
        },
        {
          type: "type",
          q: "テーブル「売上」を「カテゴリ」列でグループ化し、グループごとの金額合計を「合計金額」という列名で求めるM式を、Table.Groupを使って書いてください。",
          lang: "m",
          answer: "Table.Group(売上, {\"カテゴリ\"}, {{\"合計金額\", each List.Sum(_[金額])}})",
          hint: "Table.Group(テーブル, {グループ化列}, {{新列名, each 集計式}})という形です。集計式では_[金額]でグループ内の金額列にアクセスします。",
          e: "Table.Groupの第3引数に{{\"合計金額\", each List.Sum(_[金額])}}を渡すことで、カテゴリごとの部分テーブル_内の金額を合計した列を作れます。"
        }
      ]
    },
    {
      title: "List.Generate と List.Accumulate",
      doc: `
<p>「請求日から支払期限まで、平日だけを1日ずつ数えて期限日を求めたい」「1月から今月まで、月初日を連番で並べたリストを先に作ってから日付テーブルの土台にしたい」——こうした「決まった手続きを条件を満たすまで繰り返す」処理は、表計算のドラッグ操作では対応しにくく、M言語の<b>List.Generate</b>や<b>List.Accumulate</b>というループ的な関数の出番になります。</p>
<p><b>List.Generate(初期値の関数, 継続条件の関数, 次の値を作る関数, 任意で取り出す値の関数)</b>は、他言語の<code>while</code>ループに近い動きをします。第1引数で最初の値を作り、第2引数がtrueを返す間だけ、第3引数で次の値を作り続けます。第4引数を省略すると生成された値そのものがリストの要素になります。</p>
<pre>// 1から10までの連番リストを生成
List.Generate(() => 1, each _ &lt;= 10, each _ + 1)
// {1, 2, 3, ..., 10}

// 2026年1月1日から6か月分の月初日リストを生成
List.Generate(
    () => #date(2026, 1, 1),
    each _ &lt;= #date(2026, 6, 1),
    each Date.AddMonths(_, 1)
)</pre>
<p>一方の<b>List.Accumulate(リスト, 初期値, アキュムレーター関数)</b>は「畳み込み(fold)」と呼ばれる処理で、リストの要素を先頭から順に処理しながら、それまでの<b>累積結果</b>を次の処理に引き継いでいきます。第3引数の関数は<code>(state, current) =&gt; 次のstate</code>という形で、stateには「これまでの計算結果」、currentには「今見ている要素」が渡されます。合計値を求めるだけならList.Sumで足りますが、「1つ前までの合計を保持しながら、それぞれの行に累積値を持たせたい」といった、単純な集計関数では表現しにくい処理に向いています。</p>
<pre>// {10, 20, 30} の合計をList.Accumulateで求める(List.Sumと同じ結果になる例)
List.Accumulate({10, 20, 30}, 0, (state, current) => state + current)
// 60

// カンマ区切りのテキストに結合する
List.Accumulate({"A", "B", "C"}, "", (state, current) =>
    if state = "" then current else state & "," & current)
// "A,B,C"</pre>
<p>List.GenerateとList.Accumulateは役割が異なります。<b>List.Generate</b>は「条件を満たすまで新しい値を生み出し、リストそのものを作る」ためのもの(連番や日付リストの展開)、<b>List.Accumulate</b>は「既にあるリストを順番に読みながら、1つの累積結果に畳み込む」ためのもの(合計・連結・段階的な変換)です。どちらも書式に慣れるまでは読みにくく感じますが、繰り返し処理をUIのボタン操作だけでは表現できない場面での強力な武器になります。</p>
<p class="tip">💡 実務メモ: List.Generateで無限ループにならないよう、第2引数の継続条件は必ずいずれfalseになる式にしましょう。日付の上限や回数のカウンターを条件に含めておくと、更新が終わらなくなる事故を防げます。</p>`,
      exercises: [
        {
          type: "mc",
          q: "List.Generate(() => 1, each _ <= 5, each _ + 1) を評価した結果として正しいものはどれですか?",
          code: "List.Generate(() => 1, each _ <= 5, each _ + 1)",
          lang: "m",
          c: ["{1, 2, 3, 4, 5}(1から始めて5以下の間、1ずつ増やしながら生成する)", "{1, 2, 3, 4}(5は条件を満たさないため含まれない)", "{5}(最後に到達した値だけが返る)", "無限リストになり、決して終了しない"],
          a: 0,
          e: "List.Generateは初期値1から始まり、each _ <= 5がtrueの間だけ次の値(_ + 1)を生成し続けます。5の時点でもまだ条件を満たすため5も生成され、6になった時点で条件を満たさなくなり停止するので{1,2,3,4,5}になります。"
        },
        {
          type: "mc",
          q: "List.Accumulate({10, 20, 30}, 0, (state, current) => state + current) の第2引数「0」の役割として正しいものはどれですか?",
          code: "List.Accumulate({10, 20, 30}, 0, (state, current) => state + current)",
          lang: "m",
          c: ["畳み込み処理の最初の累積値(state)の初期値", "リストの先頭に追加される要素", "リストの要素数の上限", "アキュムレーター関数を呼び出す回数"],
          a: 0,
          e: "List.Accumulateの第2引数は畳み込みの初期状態です。ここでは0から始めて、リストの各要素を順にstateへ足し込んでいくため、最終的にList.Sumと同じ60が得られます。"
        },
        {
          type: "mc",
          q: "List.GenerateとList.Accumulateの役割の違いとして正しいものはどれですか?",
          c: ["List.Generateは条件を満たす間、新しい値を生み出して新しいリストを作るのに対し、List.Accumulateは既存のリストを順に読みながら1つの累積結果へ畳み込む", "両方とも同じ処理を行う同義語であり、置き換えても結果は変わらない", "List.Generateは既存のリストを畳み込み、List.Accumulateは新しいリストを生成する(役割が逆である)", "List.Generateはテキスト専用、List.Accumulateは数値専用の関数である"],
          a: 0,
          e: "List.Generateは「条件を満たすまで値を生成し続けてリストを作る」関数、List.Accumulateは「既にあるリストの要素を順番に処理して1つの累積結果にまとめる」関数です。前者は生成、後者は畳み込みという別の役割を持ちます。"
        },
        {
          type: "fill",
          q: "2026年1月1日から始めて、2026年6月1日以下の間、1か月ずつ増やしながら月初日のリストを生成するM式の空欄を埋めてください。",
          lang: "m",
          code: "List.Generate(\n    () => #date(2026, 1, 1),\n    each _ 【0】 #date(2026, 6, 1),\n    each Date.AddMonths(_, 【1】)\n)",
          bank: ["<=", "1", ">=", "-1"],
          answers: [0, 1],
          e: "継続条件は「2026年6月1日以下の間」なので<=を使います。次の値を作る部分は1か月ずつ増やすのでDate.AddMonths(_, 1)のように1を渡します。"
        },
        {
          type: "type",
          q: "リスト {\"A\", \"B\", \"C\"} の要素をカンマ区切りで1つの文字列に連結するM式を、List.Accumulateを使って書いてください(初期値は空文字列とし、状態が空文字列のときはcurrentをそのまま、それ以外はカンマで連結する)。",
          lang: "m",
          answer: "List.Accumulate({\"A\", \"B\", \"C\"}, \"\", (state, current) => if state = \"\" then current else state & \",\" & current)",
          hint: "第2引数は初期値の空文字列\"\"、第3引数の関数内でifを使ってstateが空かどうかを判定し、連結の仕方を切り替えます。",
          e: "List.Accumulateはstateに1つ前までの累積結果を保ちながらcurrentを順に処理します。最初だけカンマを付けずcurrentをそのまま採用することで、\"A,B,C\"のような自然な連結ができます。"
        }
      ]
    }
  ]
});
