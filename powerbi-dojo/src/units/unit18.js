window.DOJO_UNITS = window.DOJO_UNITS || [];
window.DOJO_UNITS.push({
  id: "unit18",
  no: 8,
  track: "advanced",
  icon: "🧠",
  name: "DAX応用 I: コンテキスト完全理解",
  color: "#2f6fd0",
  desc: "コンテキスト遷移・ALL族・FILTER設計・テーブル関数ツールボックス・変数とデバッグ術まで、「販売サンプル」モデルでDAXの応用的なコンテキスト操作を正面から理解する",
  lessons: [
    {
      title: "コンテキスト遷移の原理",
      doc: `
<p>商品テーブルに計算列を追加し、「その商品だけの売上合計を出したい」と思って、深く考えずに <code>[売上合計]</code> とだけ書いてみたら、なぜか商品ごとにちゃんと違う値が入った——そんな経験はないでしょうか。計算列は行コンテキストの中で動くはずなのに、フィルターコンテキストを前提に作られたメジャーがそのまま使えてしまう。この不思議な橋渡しの正体が、DAXの中でも特に理解が難しいとされる<b>コンテキスト遷移(Context Transition)</b>です。</p>
<p>コンテキスト遷移とは、<b>行コンテキストの中でCALCULATEが評価されたとき、現在処理中の行の各列の値が、そのまま同等のフィルターコンテキストに変換される</b>という仕組みのことです。ここで見逃せない規則が、「<b>メジャーへの参照は常に暗黙のCALCULATEとして扱われる</b>」という約束事です。<code>[売上合計]</code>のようにメジャーをただ書くだけでも、DAXの内部ではCALCULATE([売上合計])と書いたのと完全に同じ扱いになります。だからこそ商品テーブルの計算列で<code>[売上合計]</code>と書くだけで、コンテキスト遷移が起こり、その行(商品)のIDがフィルター条件に変換され、結果としてその商品だけの売上合計が返るのです。</p>
<pre>// 商品テーブルの計算列
商品別売上 = [売上合計]

// ↑これは、下記を明示的に書いたのとまったく同じ意味
商品別売上_明示 = CALCULATE([売上合計])</pre>
<p>ここで注意したいのが「重複行の罠」です。コンテキスト遷移は行を一意に区別する隠れたIDのようなものではなく、<b>その行が持つ各列の値をそのままフィルター条件に変換しているだけ</b>です。そのため、もし全列の値が完全に一致する重複行がテーブルに存在すると、コンテキスト遷移によるフィルターはその重複行すべてに同時にマッチしてしまい、意図せず合算されてしまうことがあります。さらに、コンテキスト遷移には「遷移のコスト」もあります。<code>SUMX(売上, [粗利])</code>のように、イテレーターの中で別の集計済みメジャーを呼び出すと、行を処理するたびにコンテキスト遷移(暗黙のCALCULATE)が発生し、行数分のオーバーヘッドが積み重なります。</p>
<pre>非効率な例 = SUMX(売上, [粗利])   // 売上の行数だけコンテキスト遷移が発生する
効率的な例 = [粗利]                // すでに全体を集計するメジャーがあるなら、そのまま使えばよい</pre>
<p class="tip">💡 実務メモ: 計算列やイテレーターの中でメジャーを参照するたびに、頭の中で「ここでコンテキスト遷移が起きている」と意識する癖をつけましょう。特に大きなファクトテーブルをイテレートしながら別のメジャーを何度も呼び出す設計は、行数分のコンテキスト遷移コストがそのままレポートの重さに直結します。</p>`,
      exercises: [
        {
          type: "mc",
          q: "DAXにおける「コンテキスト遷移」の説明として正しいものはどれですか?",
          c: [
            "行コンテキストの中でCALCULATE(暗黙のCALCULATEを含む)が評価されると、現在の行の各列の値がそのまま同等のフィルターコンテキストに変換される仕組み",
            "フィルターコンテキストが行コンテキストに変換され、以後その行コンテキストの中でしか式が評価できなくなる仕組み",
            "スライサーの選択が、ビジュアルの行・列の見出しに自動的に反映される仕組み",
            "CALCULATEを使わない限り、行コンテキストとフィルターコンテキストが混在することは一切ない"
          ],
          a: 0,
          e: "コンテキスト遷移は、行コンテキストの中でCALCULATE(明示・暗黙を問わず)が評価されたときに、その行の列の値をそのままフィルターコンテキストに変換する仕組みです。この変換によって、計算列やイテレーターの中でメジャーを安全に参照できます。"
        },
        {
          type: "mc",
          q: "商品テーブルに計算列を追加し、その式を単に <code>[売上合計]</code> とだけ書いた場合、各行にはどんな値が入りますか?",
          code: "商品別売上 = [売上合計]",
          lang: "dax",
          c: [
            "その行の商品IDでフィルターされた状態で[売上合計]が暗黙的にCALCULATEされ、その商品だけの売上合計が入る",
            "全商品の売上合計が、行によらず同じ値としてすべての行に入る",
            "エラーになる。計算列の中でメジャーを参照することはできない",
            "商品テーブルには売上列がないため、常に空白(BLANK)になる"
          ],
          a: 0,
          e: "計算列の中でメジャーを参照すると暗黙のCALCULATE、すなわちコンテキスト遷移が発生します。行コンテキストにある商品IDがフィルター条件に変換されるため、その商品だけの売上合計が返ります。"
        },
        {
          type: "mc",
          q: "コンテキスト遷移についての「重複行の罠」の説明として最も正しいものはどれですか?",
          c: [
            "コンテキスト遷移は行を一意に識別するIDではなく、その行の各列の値をそのままフィルター条件に変換するため、全列の値が完全に一致する重複行が存在すると、それらすべてが同時にフィルターされ、意図せず重複分を合算してしまうことがある",
            "コンテキスト遷移は重複行を自動的に検出して、常に1行だけを残すように重複排除してから計算する",
            "重複行があるとPower BIの読み込み時にエラーになるため、コンテキスト遷移とは無関係な話である",
            "コンテキスト遷移は列の値を無視し、常に物理的な行番号だけでフィルターするため、重複行があっても問題は起きない"
          ],
          a: 0,
          e: "コンテキスト遷移は行の各列の値をフィルター条件に変換するだけで、行を一意に区別する隠れたIDのようなものは使いません。そのため列の値がすべて同じ重複行があると、その重複行すべてが同時にマッチしてしまう点に注意が必要です。"
        },
        {
          type: "mc",
          q: "<code>SUMX(売上, [粗利])</code> のように、イテレーターの中で集計済みのメジャーをさらに呼び出すとどうなりますか?(売上テーブルには一意な売上IDがあり、各行は自分自身にしかマッチしません)",
          code: "SUMX(売上, [粗利])",
          lang: "dax",
          c: [
            "呼び出しのたびにコンテキスト遷移(暗黙のCALCULATE)が発生する。売上IDが行ごとに一意なため計算結果は[粗利]をそのまま参照した場合と一致するが、行数分のコンテキスト遷移が余分に発生し遅くなる",
            "SUMXの中でメジャーを呼び出すと必ず構文エラーになる",
            "コンテキスト遷移は1回のクエリにつき自動的に1回しか発生しないため、行数に関係なく高速に処理される",
            "[粗利]の定義そのものが書き換えられ、以後すべてのメジャーで[粗利]の挙動が変わってしまう"
          ],
          a: 0,
          e: "行コンテキストの中でメジャーを参照するたびにコンテキスト遷移が起こるため、SUMXのようなイテレーターの中で別のメジャーを呼ぶと、行数分だけ余分なコンテキスト遷移コストがかかります。この例では売上IDが一意なので結果自体は[粗利]と一致しますが、無駄に遅くなります。"
        },
        {
          type: "fill",
          q: "商品テーブルの計算列で、暗黙のコンテキスト遷移によって[売上合計]を参照するのと同じ結果になる、明示的なCALCULATEの書き方の空欄を埋めてください。",
          lang: "dax",
          code: "商品別売上_明示 = 【0】(【1】)",
          bank: ["CALCULATE", "[売上合計]", "FILTER", "ALL(商品)"],
          answers: [0, 1],
          e: "行コンテキストの中でメジャーを参照すると暗黙にCALCULATEでラップされます(コンテキスト遷移)。これを明示的に書くとCALCULATE([売上合計])となり、フィルター引数を追加しなくても現在の行(商品)の値がそのままフィルターとして働きます。"
        },
        {
          type: "type",
          q: "商品テーブルの計算列として、コンテキスト遷移(暗黙のCALCULATE)を利用して、各商品行の[粗利]を返す列「商品別粗利」を書いてください(CALCULATEを明示的に書く必要はありません)。",
          lang: "dax",
          scaffold: "商品別粗利 = ",
          answer: "商品別粗利 = [粗利]",
          accept: ["商品別粗利 = CALCULATE([粗利])"],
          hint: "行コンテキストの中でメジャーをそのまま参照するだけで、暗黙のCALCULATEによるコンテキスト遷移が起こります。",
          e: "商品テーブルの計算列の中で[粗利]というメジャーを参照すると、暗黙のCALCULATE(コンテキスト遷移)によって、その行の商品IDでフィルターされた状態で[粗利]が再評価され、商品ごとの粗利が返ります。"
        }
      ]
    },
    {
      title: "ALL族完全版",
      doc: `
<p>「カテゴリ別の売上構成比を出したい」と頼まれてDIVIDEの分母を作ろうとしたとき、初心者がまず戸惑うのが「今かかっているフィルターを、どうやって解除すればいいのか」という問題です。ここで登場するのが<b>ALL族</b>と呼ばれる一連の関数群、ALL・ALLEXCEPT・ALLSELECTED・ALLNOBLANKROW・REMOVEFILTERSです。どれも「フィルターを取り除く」役割を持ちますが、取り除き方の範囲や粒度がそれぞれ異なります。</p>
<p>まず土台となる<b>ALL(テーブルまたは列)</b>には、二面性があります。単独のテーブル関数として使うと、現在のフィルターを無視した「すべての行」を返します(たとえばCOUNTROWS(ALL(商品))で商品の全行数を数えられます)。一方、CALCULATEのフィルター引数として渡すと、その対象(テーブル全体、または指定した列)にかかっている既存のフィルターを取り除く指示として働きます。「テーブルを返す関数」と「フィルターを解除する指示」という2つの顔を持つ点が、ALLを理解するうえでの最初のハードルです。<b>REMOVEFILTERS</b>はこのうち「フィルターを解除する」役割だけに特化した、より意図が伝わりやすい名前の関数で、CALCULATEの中で使う点はALLと同じです。</p>
<pre>全体比 = DIVIDE([売上合計], CALCULATE([売上合計], REMOVEFILTERS(商品)))

全体比_ALL版 = DIVIDE([売上合計], CALCULATE([売上合計], ALL(商品)))</pre>
<p>「カテゴリの絞り込みだけは残したまま、それ以外のフィルターを解除したい」という場面では<b>ALLEXCEPT(テーブル, 除外しない列1, …)</b>を使います。これはALLとは逆に、「これだけは残す」列を指定する関数で、指定した列以外のフィルターをすべて解除します。ビジュアルの行・列の内側の絞り込みだけを解除し、スライサーなどページの外側からの絞り込みは維持したいときは<b>ALLSELECTED(テーブルまたは列)</b>の出番です。ビジュアル内での構成比(そのビジュアルが表示している範囲内での割合)を作るのに向いています。また、参照整合性の問題(売上が参照する商品IDが商品テーブルに存在しない、など)があると、Power BIは見えない空白行を自動的に追加することがありますが、<b>ALLNOBLANKROW</b>はALLと同じくフィルターを解除しつつ、この空白行だけは含めずに返します。</p>
<pre>カテゴリ内シェア = DIVIDE([売上合計], CALCULATE([売上合計], ALLEXCEPT(商品, 商品[カテゴリ])))

ビジュアル内シェア = DIVIDE([売上合計], CALCULATE([売上合計], ALLSELECTED(商品)))</pre>
<p class="tip">💡 実務メモ: 「全体比を作りたいだけなのに、ALLとALLSELECTEDのどちらを使うべきか分からない」と迷ったら、「スライサーで絞り込んだ範囲まで含めて100%としたいか(ALLSELECTED)、モデル全体を100%としたいか(ALL/REMOVEFILTERS)」を自問しましょう。この一言で用途はほぼ一意に決まります。</p>`,
      exercises: [
        {
          type: "mc",
          q: "ALL関数の「二面性」についての説明として正しいものはどれですか?",
          c: [
            "ALL(テーブルまたは列)は、単独ではフィルターを無視した「すべての行」を返すテーブル関数として使え、CALCULATEのフィルター引数として渡すと、その対象からフィルターを取り除く指示として働く",
            "ALLはCALCULATEの中でしか使えない関数であり、単独のテーブル関数として使うことはできない",
            "ALLは常に元のテーブルの行数を2倍に増やしてから返す関数である",
            "ALLは列の値を昇順に並べ替えるためだけの関数であり、フィルターとは無関係である"
          ],
          a: 0,
          e: "ALLは単独ではフィルターを無視した行の集合を返すテーブル関数として、CALCULATEの中ではその範囲のフィルターを取り除く指示として働く、という2つの顔を持ちます。この二面性がALL族を理解する鍵です。"
        },
        {
          type: "mc",
          q: "<code>CALCULATE([売上合計], ALLEXCEPT(商品, 商品[カテゴリ]))</code> の働きとして正しいものはどれですか?",
          code: "CALCULATE([売上合計], ALLEXCEPT(商品, 商品[カテゴリ]))",
          lang: "dax",
          c: [
            "商品テーブルにかかっているフィルターのうち、カテゴリ列以外のフィルター(商品名など)をすべて解除し、カテゴリ列のフィルターだけは維持したまま[売上合計]を計算する",
            "商品テーブルのカテゴリ列のフィルターだけを解除し、それ以外の列のフィルターはすべて維持する",
            "商品テーブルとは無関係に、顧客テーブルのフィルターをすべて解除する",
            "商品テーブルのカテゴリ列の値を強制的に空欄(BLANK)にしてから計算する"
          ],
          a: 0,
          e: "ALLEXCEPTは指定した列以外のフィルターを解除し、指定した列(ここではカテゴリ)のフィルターだけを維持します。ALLとは逆に、「これだけは残す」列を指定する関数です。"
        },
        {
          type: "mc",
          q: "行列ビジュアルの各行に「カテゴリごとの構成比」を表示したいとき、<code>DIVIDE([売上合計], CALCULATE([売上合計], ALLSELECTED(商品)))</code> のようにALLSELECTEDを使う理由として正しいものはどれですか?",
          code: "DIVIDE([売上合計], CALCULATE([売上合計], ALLSELECTED(商品)))",
          lang: "dax",
          c: [
            "ビジュアルの行・列で使われているカテゴリなどの内部的な絞り込みだけを解除し、ページ上のスライサーなど外側からの絞り込みは維持したまま分母(全体)を計算できるため",
            "ページ上のスライサーも含めて、モデル内のすべてのフィルターを完全に解除できるため(ALLと完全に同じ動作をするため)",
            "ALLSELECTEDは常にALLよりも実行速度が速いため、パフォーマンス上の理由だけで使う",
            "ALLSELECTEDを使うとビジュアルの行・列の並び順が自動的にアルファベット順に固定されるため"
          ],
          a: 0,
          e: "ALLSELECTEDはビジュアルの内部的な絞り込み(行・列に使われている値)だけを解除し、ページのスライサーなど外側からの絞り込みは維持します。そのため「スライサーで選んだ範囲内での構成比」を計算するのに向いています。"
        },
        {
          type: "mc",
          q: "ALLNOBLANKROWとALLの違いとして正しいものはどれですか?",
          c: [
            "ALLNOBLANKROWは、参照整合性の問題(売上のキーに対応する商品が見つからない等)によって自動的に追加される見えない空白行を除いてフィルターを解除するが、ALLはその空白行も含めて解除する",
            "ALLNOBLANKROWはテーブルの空白セルをすべて0に置き換えてから解除する関数である",
            "ALLNOBLANKROWとALLはまったく同じ動作をする単なる別名であり、違いはない",
            "ALLNOBLANKROWはALLよりも常に多くの行を返す関数である"
          ],
          a: 0,
          e: "モデルに参照整合性の問題があると、Power BIは見えない空白行を自動的に追加することがあります。ALLNOBLANKROWはこの空白行を除いてフィルターを解除するため、正確な件数計算などに使えます。"
        },
        {
          type: "fill",
          q: "商品のカテゴリだけを維持し、それ以外の商品テーブルのフィルターを解除して売上合計を計算する「カテゴリ内シェア分母」メジャーの空欄を埋めてください。",
          lang: "dax",
          code: "カテゴリ内シェア分母 = CALCULATE([売上合計], 【0】(商品, 商品[カテゴリ]))",
          bank: ["ALLEXCEPT", "ALLSELECTED", "REMOVEFILTERS", "ALLNOBLANKROW"],
          answers: [0],
          e: "ALLEXCEPTは第2引数以降で指定した列のフィルターだけを維持し、それ以外の商品テーブルのフィルターを解除します。カテゴリだけ残したいこの例にちょうど合う関数です。"
        },
        {
          type: "type",
          q: "商品全体に対する売上合計に占める、現在のフィルターコンテキストでの売上合計の割合を計算するメジャー「全体比」を、REMOVEFILTERSを使って書いてください。",
          lang: "dax",
          scaffold: "全体比 = ",
          answer: "全体比 = DIVIDE([売上合計], CALCULATE([売上合計], REMOVEFILTERS(商品)))",
          hint: "分子は現在のフィルターのままの[売上合計]、分母はCALCULATEの中でREMOVEFILTERS(商品)を使って商品のフィルターを取り除いた[売上合計]です。DIVIDEで割り算します。",
          e: "REMOVEFILTERSは商品テーブルにかかっているフィルターを取り除くため、分母は商品カテゴリなどの絞り込みに関係なく全体の売上合計になります。DIVIDEで割ることで0除算も安全に処理できます。"
        }
      ]
    },
    {
      title: "FILTER設計と性能",
      doc: `
<p>「単価が1000円を超える商品だけの売上合計を出して」と頼まれたとき、これまで学んだ<code>CALCULATE([売上合計], 顧客[地域] = "東京")</code>のようなブールフィルターの書き方では対応できません。ブールフィルターは基本的に1つの列への単純な比較しか書けないからです。ここで使うのが、テーブルを1行ずつ調べながら条件に合う行だけを残す<b>FILTER(テーブル, 条件)</b>です。</p>
<p>FILTER自体もSUMXなどと同じ<b>イテレーター</b>であり、指定したテーブルの行コンテキストを1行ずつ作りながら条件式を評価し、条件を満たした行だけを残した新しいテーブルを返します。この性質のおかげで、複数列にまたがる条件やAND・OR・関数呼び出しを含む複雑な条件でも自由に組み立てられます。<code>FILTER(ALL(商品), 商品[単価] &gt; 1000)</code>のように、対象テーブルをまずALLで既存フィルターから解放してから絞り込むのが定番の型です。</p>
<pre>高額商品売上 = CALCULATE([売上合計], FILTER(ALL(商品), 商品[単価] &gt; 1000))</pre>
<p>ここで性能上の注意点があります。「単価が1000円を超える商品」を求めるだけなら、商品テーブル(ディメンション)を絞り込んでからリレーション経由で売上(ファクト)側を絞り込むほうが有利です。一般にディメンションテーブルはファクトテーブルよりずっと行数が少ないため、行数の少ない側をFILTERでイテレートするほうが、大きなファクトテーブルを直接1行ずつ調べるより処理コストを抑えやすいのです。もう1つ覚えておきたいのが<b>KEEPFILTERS</b>です。CALCULATEのフィルター引数は通常、対象の列に既にかかっているフィルターを<b>置き換え(上書き)</b>ますが、フィルター引数をKEEPFILTERSで包むと、既存のフィルターを上書きせず、新しい条件と<b>AND(交差)</b>で組み合わせるようになります。既存の絞り込みをさらに狭める方向にしか働かない点が、通常のCALCULATEとの大きな違いです。</p>
<pre>プレミアム顧客だけに絞り込み = CALCULATE([売上合計], KEEPFILTERS(顧客[会員ランク] = "プレミアム"))</pre>
<p class="tip">💡 実務メモ: FILTERを書くときは「何行に対して評価が走るか」を意識しましょう。特にFILTER(売上, RELATED(商品[単価]) &gt; 1000)のようにファクトテーブルを直接調べる書き方は、同じ結果でもFILTER(ALL(商品), 商品[単価] &gt; 1000)より重くなりがちです。可能な限り小さいテーブル側で絞り込む癖をつけると、レポートの体感速度が変わってきます。</p>`,
      exercises: [
        {
          type: "mc",
          q: "<code>CALCULATE([売上合計], 顧客[地域] = \"東京\")</code> のようなブールフィルターと、<code>CALCULATE([売上合計], FILTER(ALL(商品), 商品[単価] &gt; 1000))</code> のようなFILTER+テーブルの書き方の違いとして正しいものはどれですか?",
          c: [
            "ブールフィルターは基本的に1つの列に対する単純な比較しか書けないが、FILTER(テーブル, 条件)は指定したテーブルを1行ずつイテレートしながら、複数列にまたがる条件や複雑な論理式を自由に書ける",
            "ブールフィルターとFILTER(テーブル, 条件)はまったく同じ結果しか返せず、書き方が違うだけで用途に差はない",
            "FILTER(テーブル, 条件)はCALCULATEの中では使えず、必ずCALCULATETABLEの中でしか使えない",
            "ブールフィルターは複数列の複雑な条件を書けるが、FILTERは1つの列にしか条件を書けない"
          ],
          a: 0,
          e: "ブールフィルターは基本的に1つの列への単純な比較に向いていますが、FILTER(テーブル, 条件)はテーブルを1行ずつイテレートするため、複数列にまたがる条件やAND/OR、関数を使った複雑な条件を自由に組み立てられます。"
        },
        {
          type: "mc",
          q: "「単価が1000円を超える商品の売上合計」を求めたいとき、<code>FILTER(ALL(商品), 商品[単価] &gt; 1000)</code>のように商品テーブルを絞り込んでからCALCULATEに渡す設計が、売上テーブルを直接イテレートするより好ましいとされる理由として正しいものはどれですか?",
          c: [
            "一般に商品のようなディメンションテーブルは売上のようなファクトテーブルより行数が少なく、少ない行数のテーブルをFILTERでイテレートしてからリレーションでファクト側を絞り込むほうが処理コストを抑えやすいため",
            "ディメンションテーブルにはRELATED関数が存在しないため、ファクトテーブルを使わざるを得ないため",
            "ファクトテーブルには単価列が存在しないため、技術的にファクトテーブルでは絞り込めないため",
            "FILTERはディメンションテーブルに対してしか使えない関数だから"
          ],
          a: 0,
          e: "一般にディメンションテーブルはファクトテーブルより行数がずっと少ないため、行数の少ない側をFILTERでイテレートしてリレーション経由でファクト側を絞り込むほうが、大きなファクトテーブルを直接1行ずつ調べるより効率的です。"
        },
        {
          type: "mc",
          q: "<code>CALCULATE([売上合計], KEEPFILTERS(顧客[会員ランク] = \"プレミアム\"))</code> のようにKEEPFILTERSでフィルター引数を包んだ場合の挙動として正しいものはどれですか?",
          code: "CALCULATE([売上合計], KEEPFILTERS(顧客[会員ランク] = \"プレミアム\"))",
          lang: "dax",
          c: [
            "会員ランク列にすでにかかっているフィルター(スライサーなど)を上書きせず、それと「プレミアム」という条件をAND(交差)で組み合わせる",
            "会員ランク列にすでにかかっているフィルターを完全に無視し、常にプレミアムの顧客だけに絞り込む(通常のCALCULATEと同じ動作になる)",
            "KEEPFILTERSを使うと、会員ランク以外のすべての列のフィルターも同時に解除される",
            "KEEPFILTERSはCALCULATEの外側では使えず、ここでは構文エラーになる"
          ],
          a: 0,
          e: "KEEPFILTERSでフィルター引数を包むと、通常のCALCULATEのように既存のフィルターを置き換えるのではなく、既存のフィルターと新しい条件をANDで交差させます。既存の絞り込みをさらに狭める方向にしか働きません。"
        },
        {
          type: "fill",
          q: "商品の単価が1000円を超える商品だけに絞り込んだ売上合計を求める「高額商品売上」メジャーの空欄を埋めてください。",
          lang: "dax",
          code: "高額商品売上 = CALCULATE([売上合計], 【0】(【1】(商品), 商品[単価] > 1000))",
          bank: ["FILTER", "ALL", "CALCULATE", "VALUES"],
          answers: [0, 1],
          e: "FILTER(ALL(商品), …)は、商品テーブルの既存フィルターを無視した全商品を1行ずつ調べ、単価が1000円を超える行だけを残したテーブルを作ります。これをCALCULATEに渡すことで、その商品群だけに絞り込んだ売上合計を計算できます。"
        },
        {
          type: "type",
          q: "顧客の会員ランクにすでにかかっているフィルターを上書きせず、それに加えて「プレミアム」の顧客だけにさらに絞り込む売上合計メジャー「プレミアム顧客だけに絞り込み」を、KEEPFILTERSを使って書いてください。",
          lang: "dax",
          scaffold: "プレミアム顧客だけに絞り込み = ",
          answer: "プレミアム顧客だけに絞り込み = CALCULATE([売上合計], KEEPFILTERS(顧客[会員ランク] = \"プレミアム\"))",
          hint: "CALCULATEのフィルター引数をKEEPFILTERSで包むと、既存のフィルターを上書きせずAND条件として交差させられます。",
          e: "KEEPFILTERSで包むことで、既存の会員ランクフィルターを置き換えるのではなく、そこに「プレミアム」という条件をAND(交差)で追加できます。既存の絞り込みの中でさらに絞り込みたい場面に向いています。"
        }
      ]
    },
    {
      title: "テーブル関数ツールボックス",
      doc: `
<p>上司から「カテゴリごとの売上合計を、カテゴリ名と数値のペアの一覧として見たい」「地域と会員ランクの全組み合わせで表を作りたい」といった、ビジュアルに頼らずDAXの中だけでテーブルそのものを組み立てたい場面が出てきます。ここで役立つのが、VALUES・DISTINCT・SUMMARIZE・ADDCOLUMNS・SELECTCOLUMNS・CROSSJOIN・GENERATE・UNIONという<b>テーブルを返す関数</b>のツールボックスです。</p>
<p><b>VALUES(列)</b>と<b>DISTINCT(列)</b>はどちらも重複のない値の一覧を返しますが、実は同じではありません。参照整合性の問題(リレーション先に対応する行がない)があると、VALUESは自動生成される見えない空白行を含むことがあるのに対し、DISTINCTは実際のデータに存在する値だけを返します。グループ化と集計値の追加を同時に行いたいとき、<b>SUMMARIZE(テーブル, グループ化列, …, 名前, 式, …)</b>で集計列(拡張引数)まで一気に書きたくなりますが、その拡張引数は想定と異なるフィルターコンテキストで評価されることがあるため、一般にはSUMMARIZEをグループ化専用に使い、集計値の追加は<b>ADDCOLUMNS(テーブル, 名前, 式, …)</b>と組み合わせるほうが安全とされています。似た働きの<b>SELECTCOLUMNS(テーブル, 名前, 式, …)</b>は、既存の列や式に好きな名前を付けて選び出す、SQLのSELECTに近い関数です。</p>
<pre>カテゴリ一覧 = VALUES(商品[カテゴリ])

カテゴリ別売上サマリー = ADDCOLUMNS(VALUES(商品[カテゴリ]), "カテゴリ別売上", [売上合計])

顧客地域一覧 = SELECTCOLUMNS(顧客, "地域名", 顧客[地域])</pre>
<p>複数テーブルを組み合わせる関数もあります。<b>CROSSJOIN(テーブル1, テーブル2, …)</b>は2つ以上のテーブルの単純な総当たり(デカルト積)を作ります。一方<b>GENERATE(テーブル1, テーブル2式)</b>は、テーブル1の各行の行コンテキストの中でテーブル2式を評価するため、行ごとに条件が変わる相関サブクエリのような、より柔軟な組み合わせを作れます。<b>UNION(テーブル1, テーブル2, …)</b>は同じ列数のテーブルどうしを、列名ではなく位置で対応付けて積み上げる関数で、SQLのUNION ALLのように重複行もそのまま残ります。なお、ビジュアルの裏側でPower BIが実際に発行しているクエリの多くは<b>SUMMARIZECOLUMNS</b>という関数ですが、この関数は非常に高機能な反面、通常のメジャーや計算列の中では使えません。一方、計算テーブルの定義や、EVALUATE文を書くDAXクエリの中では問題なく使えます(計算テーブルでの利用はむしろ定番です)。</p>
<pre>全カテゴリ×全地域の組み合わせ = CROSSJOIN(VALUES(商品[カテゴリ]), VALUES(顧客[地域]))

全地域と追加行 = UNION(VALUES(顧客[地域]), ROW("地域", "海外"))</pre>
<p class="tip">💡 実務メモ: SUMMARIZEに集計列を詰め込んだ複雑な式を見かけたら、ADDCOLUMNS(SUMMARIZE(テーブル, グループ化列), 名前, 式)のように役割を分割できないか検討してみましょう。「グループ化はSUMMARIZE、集計値の追加はADDCOLUMNS」と役割を分けるだけで、意図しないフィルターコンテキストのすれ違いをかなり防げます。</p>`,
      exercises: [
        {
          type: "mc",
          q: "VALUES(列)とDISTINCT(列)の違いとして正しいものはどれですか?",
          c: [
            "どちらも指定した列の重複のない値を返す点は同じだが、VALUESはリレーション先に対応する行がない場合に自動生成される見えない空白行を含むことがあるのに対し、DISTINCTは実際のデータに存在する値だけを返す",
            "VALUESは数値列にしか使えず、DISTINCTはテキスト列にしか使えない",
            "DISTINCTは常に降順で結果を返し、VALUESは常に昇順で返す",
            "VALUESとDISTINCTはまったく同じ実装であり、内部的にも一切の違いがない"
          ],
          a: 0,
          e: "VALUESとDISTINCTはどちらも重複のない値を返しますが、参照整合性の問題で自動生成される見えない空白行をVALUESは含みうるのに対し、DISTINCTは実データにある値だけを返す点が異なります。正確な件数計算ではこの違いが結果に影響することがあります。"
        },
        {
          type: "mc",
          q: "SUMMARIZE(テーブル, グループ化列, \"新しい列名\", 式)のように、SUMMARIZEの追加引数(拡張列)で集計値を計算する書き方についての注意点として正しいものはどれですか?",
          c: [
            "拡張列の式は想定と異なるフィルターコンテキストで評価されることがあり、一般的にはSUMMARIZEをグループ化専用に使い、集計列の追加はADDCOLUMNSと組み合わせるほうが安全とされている",
            "SUMMARIZEの拡張引数は常にADDCOLUMNSより高速に動作するため、集計列の追加には積極的に使うべきである",
            "SUMMARIZEに拡張引数を渡すと必ず構文エラーになるため、そもそも書くことができない",
            "SUMMARIZEの拡張引数はグループ化した列の値をすべて文字列型に強制変換してしまう"
          ],
          a: 0,
          e: "SUMMARIZEの拡張引数(集計列)は、想定と異なるフィルターコンテキストで評価される場合があり、思わぬ結果につながることがあります。SUMMARIZEはグループ化に専念させ、集計列はADDCOLUMNSで後から追加するのが安全な設計とされています。"
        },
        {
          type: "mc",
          q: "CROSSJOIN(テーブル1, テーブル2)とGENERATE(テーブル1, テーブル2式)の違いとして正しいものはどれですか?",
          c: [
            "CROSSJOINは2つのテーブルの単純な総当たり(デカルト積)を作るのに対し、GENERATEはテーブル1の各行の行コンテキストの中でテーブル2式を評価するため、行ごとに異なる(相関した)テーブル2の結果を組み合わせられる",
            "CROSSJOINとGENERATEはまったく同じ結果を返す同義語であり、書き方が違うだけである",
            "GENERATEは常に1行だけを返す関数であり、複数行のテーブルは返せない",
            "CROSSJOINはテーブル1の行コンテキストの中でテーブル2式を評価するのに対し、GENERATEは単純な総当たりを作る(説明が逆)"
          ],
          a: 0,
          e: "CROSSJOINは常に単純な総当たり(デカルト積)を作りますが、GENERATEはテーブル1の各行の行コンテキストの中でテーブル2の式を評価するため、行ごとに条件を変えた相関サブクエリのような組み合わせを作れます。"
        },
        {
          type: "mc",
          q: "SUMMARIZECOLUMNS関数についての説明として正しいものはどれですか?",
          c: [
            "グループ化・フィルター・集計列を1つでこなす強力な関数だが、通常のメジャーや計算列の中では使えない(一方、計算テーブルの定義やEVALUATE文によるDAXクエリでは使える。Power BIのビジュアルも裏側でこの関数によるクエリを生成している)",
            "SUMMARIZECOLUMNSはSUMMARIZEの完全な別名であり、メジャーの中でSUMMARIZEの代わりにいつでも使える",
            "SUMMARIZECOLUMNSは計算列専用の関数であり、メジャーやDAXクエリでは使用できない",
            "SUMMARIZECOLUMNSはPower Query(M言語)の関数であり、DAXには存在しない"
          ],
          a: 0,
          e: "SUMMARIZECOLUMNSはグループ化・フィルター・集計列を1つでこなせる強力な関数ですが、メジャーや計算列の中では使えません。一方、計算テーブルの定義やEVALUATE文によるDAXクエリでは使用できます。Power BIのビジュアルが背後で生成するクエリの多くもこの関数です。"
        },
        {
          type: "fill",
          q: "商品カテゴリごとに、そのカテゴリの売上合計を追加列として持つテーブルを作る式の空欄を埋めてください。",
          lang: "dax",
          code: "カテゴリ別売上サマリー = 【0】(【1】(商品[カテゴリ]), \"カテゴリ別売上\", [売上合計])",
          bank: ["ADDCOLUMNS", "VALUES", "SUMMARIZE", "CROSSJOIN"],
          answers: [0, 1],
          e: "VALUES(商品[カテゴリ])でカテゴリの重複のない一覧を取得し、ADDCOLUMNSでその各行(カテゴリ)ごとに[売上合計]を評価した列を追加します。SUMMARIZEの拡張引数より安全とされる組み合わせです。"
        },
        {
          type: "type",
          q: "顧客テーブルから地域列だけを取り出し、列名を「地域名」に変更したテーブルを返す式を、SELECTCOLUMNSを使って書いてください(式全体の名前は「顧客地域一覧」とします)。",
          lang: "dax",
          scaffold: "顧客地域一覧 = ",
          answer: "顧客地域一覧 = SELECTCOLUMNS(顧客, \"地域名\", 顧客[地域])",
          hint: "SELECTCOLUMNS(テーブル, \"新しい列名\", 式) の順で引数を指定します。",
          e: "SELECTCOLUMNSは元のテーブルから任意の列や式を選び、好きな名前を付けて新しいテーブルとして返します。ここでは顧客テーブルの地域列を「地域名」という名前で取り出しています。"
        }
      ]
    },
    {
      title: "変数とデバッグ術",
      doc: `
<p>複雑なDAX式を書いていると、同じCALCULATEの塊を2回も3回も書き写す羽目になったり、途中の計算結果が正しいのか確かめたくてもビジュアルの上ではブラックボックスのままだったり、という壁にぶつかります。この壁を乗り越える2つの武器が、<b>VAR ... RETURN構文</b>と、DAXクエリビューの<b>EVALUATE</b>です。</p>
<p>VARで定義した式は、その後のRETURN句や後続のVARの中で何度参照しても、DAXエンジンは基本的に<b>1回だけ評価</b>し、その結果を使い回します。同じ計算を式の中に何度も書くよりも無駄な再計算を避けられ、名前が付くことで式の意図も伝わりやすくなります。VARに代入できるのはSUMやCALCULATEのようなスカラー値だけではありません。FILTERやVALUESのような<b>テーブル式の結果</b>もVARに代入でき、後続でテーブル変数として扱えます。</p>
<pre>粗利率 =
VAR 売上高 = [売上合計]
VAR 原価計 = SUMX(売上, 売上[数量] * RELATED(商品[原価]))
RETURN
    DIVIDE(売上高 - 原価計, 売上高)</pre>
<p>DAXにはビジュアル上でブレークポイントを置くような本格的なデバッガーはありません。そこでよく使われるのが、VARに入れたテーブル変数の中身を<b>CONCATENATEX</b>で文字列として書き出し、意図した行が含まれているかを目で確認する、という簡易的なデバッグ手段です。さらにPower BI Desktopには「DAXクエリビュー」という4つ目のビューがあり、<code>EVALUATE ROW("結果", [メジャー])</code>のようなクエリを実行するだけで、ビジュアルを1つも作らずにメジャーの値を単体で確認できます。モデル自体には一切影響を与えないため、安心して式を試行錯誤できる場所です。</p>
<pre>デバッグ用 =
VAR 高額商品 = FILTER(商品, 商品[単価] > 5000)
RETURN
    CONCATENATEX(高額商品, 商品[商品名], "、")

// DAXクエリビューで実行
EVALUATE
ROW("結果", [粗利率])</pre>
<p class="tip">💡 実務メモ: メジャーの結果がおかしいと感じたら、RETURN句を一時的に確認したいVAR名だけに差し替えて実行し、そのVARが単体で何を返しているかを1つずつ検算しましょう。DAXクエリビューのEVALUATEと組み合わせれば、ビジュアルを作り直すことなく、その場で仮説を検証できます。</p>`,
      exercises: [
        {
          type: "mc",
          q: "DAXのVARで定義した式についての説明として正しいものはどれですか?",
          c: [
            "VARで定義した式は、その後のRETURN句や後続のVARの中で何度参照しても、DAXエンジンは基本的に1回だけ評価し、その結果を使い回す",
            "VARで定義した式は、参照するたびに毎回ゼロから再評価される。何度も参照すると、参照した回数だけ計算コストがかかる",
            "VARは必ずテーブル型の式しか受け付けず、スカラー値を代入することはできない",
            "VARで定義した変数は、そのメジャーの外にある他のメジャーからも直接参照できるグローバル変数になる"
          ],
          a: 0,
          e: "VARで定義した式はその内部で1回だけ評価され、結果が再利用されます。同じ計算を式の中に何度も書くよりも、無駄な再計算を避けられ、意図も明確になります。"
        },
        {
          type: "mc",
          q: "DAXのVARに代入できる値の種類として正しいものはどれですか?",
          c: [
            "SUM(…)のようなスカラー値だけでなく、FILTER(…)やVALUES(…)のようなテーブル式の結果もVARに代入でき、後続でテーブルとして扱える",
            "VARにはスカラー値(数値やテキストなど)しか代入できず、テーブル式を代入するとエラーになる",
            "VARにはテーブル式しか代入できず、SUMのようなスカラー値を代入するとエラーになる",
            "VARに代入できるのは、あらかじめ標準メジャーとして登録済みの式に限られる"
          ],
          a: 0,
          e: "VARにはスカラー値だけでなく、FILTERやVALUESなどが返すテーブルも代入できます。テーブル変数として保持しておき、後続でSUMXやCONCATENATEXなどに渡して利用できます。"
        },
        {
          type: "mc",
          q: "DAXの式が意図通りの結果になっているか確認したいとき、VARに入れたテーブルの中身をCONCATENATEXで文字列として連結して確認する、という使い方の目的として正しいものはどれですか?",
          c: [
            "DAXにはビジュアル上でブレークポイントを置くような本格的なデバッガーがないため、テーブル変数の中身を目に見える文字列として書き出し、意図した行が含まれているかを確認するための簡易的なデバッグ手段として使う",
            "CONCATENATEXを使うと、それ以降のすべての計算が自動的にキャッシュされ、処理速度が上がるため",
            "CONCATENATEXを一度でも使うと、メジャーの定義がそのまま計算列に変換される",
            "CONCATENATEXはテーブルの行数を必ず1行に強制的に減らす関数であるため、それ自体がバグ修正になる"
          ],
          a: 0,
          e: "DAXには視覚的なデバッガーがないため、VARに入れたテーブルの中身をCONCATENATEXで文字列として書き出し、意図した行や値が含まれているかを目で確認するのは実務でよく使われる簡易デバッグ手段です。"
        },
        {
          type: "mc",
          q: "Power BI DesktopのDAXクエリビューで<code>EVALUATE ROW(\"結果\", [粗利率])</code>のようなクエリを実行することについての説明として正しいものはどれですか?",
          code: "EVALUATE\nROW(\"結果\", [粗利率])",
          lang: "dax",
          c: [
            "ビジュアルを配置しなくても、EVALUATE ROW(\"結果\", [メジャー])のようなクエリを実行するだけでメジャーの値を単体で確認でき、モデル自体を変更せずに安全に検証できる",
            "DAXクエリビューでEVALUATEを実行すると、そのメジャーの定義が自動的にモデルから削除される",
            "DAXクエリビューはPower BI Serviceにレポートを発行した後でなければ一切使用できない",
            "EVALUATE文はテーブルを返す式には使えず、単一の数値を返す式にしか使えない"
          ],
          a: 0,
          e: "DAXクエリビューでは、EVALUATE ROW(\"結果\", [メジャー])のようなクエリを書くだけで、ビジュアルを作らずにメジャーを単体評価できます。モデルには影響を与えないため、安全に試行錯誤できます。"
        },
        {
          type: "fill",
          q: "単価が5000円を超える商品だけをVARに入れておき、その商品名を読点区切りで連結して確認する「デバッグ用」式の空欄を埋めてください。",
          lang: "dax",
          code: "デバッグ用 =\nVAR 高額商品 = 【0】(商品, 商品[単価] > 5000)\nRETURN\n    【1】(高額商品, 商品[商品名], \"、\")",
          bank: ["FILTER", "CONCATENATEX", "ALL", "ADDCOLUMNS"],
          answers: [0, 1],
          e: "VARに商品テーブルをFILTERで絞り込んだテーブルを格納し、RETURN句でCONCATENATEXを使ってその中身(商品名)を文字列として書き出すことで、意図した行が含まれているかを目で確認できます。"
        },
        {
          type: "type",
          q: "売上高([売上合計])から原価合計(SUMXで売上テーブルを1行ずつ処理し、数量×原価を合計したもの)を引いた額を売上高で割る「粗利率」メジャーを、VARとRETURNを使って書いてください(0除算はDIVIDEで安全に処理すること)。",
          lang: "dax",
          scaffold: "粗利率 =\nVAR 売上高 = [売上合計]\nVAR 原価計 = SUMX(売上, 売上[数量] * RELATED(商品[原価]))\nRETURN\n    ",
          answer: "粗利率 =\nVAR 売上高 = [売上合計]\nVAR 原価計 = SUMX(売上, 売上[数量] * RELATED(商品[原価]))\nRETURN\n    DIVIDE(売上高 - 原価計, 売上高)",
          hint: "RETURN句にDIVIDE(売上高 - 原価計, 売上高)と書きます。",
          e: "売上高と原価計をそれぞれVARで一度だけ評価しておき、RETURN句でDIVIDEを使って粗利率を計算します。VARを使うことで同じ式を重複して書かずに済み、読みやすくなります。"
        }
      ]
    }
  ]
});
